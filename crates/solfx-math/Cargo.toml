[package]
name = "solfx-math"
description = "Fixed-point financial primitives for the SolFX protocol. No floats, no panics, no I/O."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]

[dev-dependencies]
proptest = "1.8"
