/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findLpPoolPda,
  findProtocolPda,
  findWithdrawRequestPda,
} from "../pdas";
import { SOLFX_CORE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REQUEST_REMOVE_LIQUIDITY_DISCRIMINATOR = new Uint8Array([
  236, 205, 93, 220, 247, 64, 110, 105,
]);

export function getRequestRemoveLiquidityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REQUEST_REMOVE_LIQUIDITY_DISCRIMINATOR,
  );
}

export type RequestRemoveLiquidityInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountProtocol extends string | AccountMeta<string> = string,
  TAccountLpPool extends string | AccountMeta<string> = string,
  TAccountWithdrawRequest extends string | AccountMeta<string> = string,
  TAccountProviderLpAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProvider extends string
        ? WritableSignerAccount<TAccountProvider> &
            AccountSignerMeta<TAccountProvider>
        : TAccountProvider,
      TAccountProtocol extends string
        ? ReadonlyAccount<TAccountProtocol>
        : TAccountProtocol,
      TAccountLpPool extends string
        ? WritableAccount<TAccountLpPool>
        : TAccountLpPool,
      TAccountWithdrawRequest extends string
        ? WritableAccount<TAccountWithdrawRequest>
        : TAccountWithdrawRequest,
      TAccountProviderLpAccount extends string
        ? ReadonlyAccount<TAccountProviderLpAccount>
        : TAccountProviderLpAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RequestRemoveLiquidityInstructionData = {
  discriminator: ReadonlyUint8Array;
  lpAmount: bigint;
};

export type RequestRemoveLiquidityInstructionDataArgs = {
  lpAmount: number | bigint;
};

export function getRequestRemoveLiquidityInstructionDataEncoder(): FixedSizeEncoder<RequestRemoveLiquidityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["lpAmount", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: REQUEST_REMOVE_LIQUIDITY_DISCRIMINATOR,
    }),
  );
}

export function getRequestRemoveLiquidityInstructionDataDecoder(): FixedSizeDecoder<RequestRemoveLiquidityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["lpAmount", getU64Decoder()],
  ]);
}

export function getRequestRemoveLiquidityInstructionDataCodec(): FixedSizeCodec<
  RequestRemoveLiquidityInstructionDataArgs,
  RequestRemoveLiquidityInstructionData
> {
  return combineCodec(
    getRequestRemoveLiquidityInstructionDataEncoder(),
    getRequestRemoveLiquidityInstructionDataDecoder(),
  );
}

export type RequestRemoveLiquidityAsyncInput<
  TAccountProvider extends string = string,
  TAccountProtocol extends string = string,
  TAccountLpPool extends string = string,
  TAccountWithdrawRequest extends string = string,
  TAccountProviderLpAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  provider: TransactionSigner<TAccountProvider>;
  protocol?: Address<TAccountProtocol>;
  lpPool?: Address<TAccountLpPool>;
  /**
   * One request per provider. A second one while the first is outstanding fails on
   * `init` — which is the right behaviour: two requests would need two unlock clocks and
   * let a provider keep a settled request permanently available by rolling it forward.
   */
  withdrawRequest?: Address<TAccountWithdrawRequest>;
  providerLpAccount: Address<TAccountProviderLpAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  lpAmount: RequestRemoveLiquidityInstructionDataArgs["lpAmount"];
};

export async function getRequestRemoveLiquidityInstructionAsync<
  TAccountProvider extends string,
  TAccountProtocol extends string,
  TAccountLpPool extends string,
  TAccountWithdrawRequest extends string,
  TAccountProviderLpAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: RequestRemoveLiquidityAsyncInput<
    TAccountProvider,
    TAccountProtocol,
    TAccountLpPool,
    TAccountWithdrawRequest,
    TAccountProviderLpAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RequestRemoveLiquidityInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountProtocol,
    TAccountLpPool,
    TAccountWithdrawRequest,
    TAccountProviderLpAccount,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    provider: { value: input.provider ?? null, isWritable: true },
    protocol: { value: input.protocol ?? null, isWritable: false },
    lpPool: { value: input.lpPool ?? null, isWritable: true },
    withdrawRequest: { value: input.withdrawRequest ?? null, isWritable: true },
    providerLpAccount: {
      value: input.providerLpAccount ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocol.value) {
    accounts.protocol.value = await findProtocolPda();
  }
  if (!accounts.lpPool.value) {
    accounts.lpPool.value = await findLpPoolPda();
  }
  if (!accounts.withdrawRequest.value) {
    accounts.withdrawRequest.value = await findWithdrawRequestPda({
      provider: expectAddress(accounts.provider.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.provider),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.lpPool),
      getAccountMeta(accounts.withdrawRequest),
      getAccountMeta(accounts.providerLpAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRequestRemoveLiquidityInstructionDataEncoder().encode(
      args as RequestRemoveLiquidityInstructionDataArgs,
    ),
    programAddress,
  } as RequestRemoveLiquidityInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountProtocol,
    TAccountLpPool,
    TAccountWithdrawRequest,
    TAccountProviderLpAccount,
    TAccountSystemProgram
  >);
}

export type RequestRemoveLiquidityInput<
  TAccountProvider extends string = string,
  TAccountProtocol extends string = string,
  TAccountLpPool extends string = string,
  TAccountWithdrawRequest extends string = string,
  TAccountProviderLpAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  provider: TransactionSigner<TAccountProvider>;
  protocol: Address<TAccountProtocol>;
  lpPool: Address<TAccountLpPool>;
  /**
   * One request per provider. A second one while the first is outstanding fails on
   * `init` — which is the right behaviour: two requests would need two unlock clocks and
   * let a provider keep a settled request permanently available by rolling it forward.
   */
  withdrawRequest: Address<TAccountWithdrawRequest>;
  providerLpAccount: Address<TAccountProviderLpAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  lpAmount: RequestRemoveLiquidityInstructionDataArgs["lpAmount"];
};

export function getRequestRemoveLiquidityInstruction<
  TAccountProvider extends string,
  TAccountProtocol extends string,
  TAccountLpPool extends string,
  TAccountWithdrawRequest extends string,
  TAccountProviderLpAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: RequestRemoveLiquidityInput<
    TAccountProvider,
    TAccountProtocol,
    TAccountLpPool,
    TAccountWithdrawRequest,
    TAccountProviderLpAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RequestRemoveLiquidityInstruction<
  TProgramAddress,
  TAccountProvider,
  TAccountProtocol,
  TAccountLpPool,
  TAccountWithdrawRequest,
  TAccountProviderLpAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    provider: { value: input.provider ?? null, isWritable: true },
    protocol: { value: input.protocol ?? null, isWritable: false },
    lpPool: { value: input.lpPool ?? null, isWritable: true },
    withdrawRequest: { value: input.withdrawRequest ?? null, isWritable: true },
    providerLpAccount: {
      value: input.providerLpAccount ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.provider),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.lpPool),
      getAccountMeta(accounts.withdrawRequest),
      getAccountMeta(accounts.providerLpAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRequestRemoveLiquidityInstructionDataEncoder().encode(
      args as RequestRemoveLiquidityInstructionDataArgs,
    ),
    programAddress,
  } as RequestRemoveLiquidityInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountProtocol,
    TAccountLpPool,
    TAccountWithdrawRequest,
    TAccountProviderLpAccount,
    TAccountSystemProgram
  >);
}

export type ParsedRequestRemoveLiquidityInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    provider: TAccountMetas[0];
    protocol: TAccountMetas[1];
    lpPool: TAccountMetas[2];
    /**
     * One request per provider. A second one while the first is outstanding fails on
     * `init` — which is the right behaviour: two requests would need two unlock clocks and
     * let a provider keep a settled request permanently available by rolling it forward.
     */
    withdrawRequest: TAccountMetas[3];
    providerLpAccount: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: RequestRemoveLiquidityInstructionData;
};

export function parseRequestRemoveLiquidityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRequestRemoveLiquidityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      provider: getNextAccount(),
      protocol: getNextAccount(),
      lpPool: getNextAccount(),
      withdrawRequest: getNextAccount(),
      providerLpAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRequestRemoveLiquidityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
