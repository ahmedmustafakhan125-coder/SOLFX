/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findProtocolPda, findUserAccountPda } from "../pdas";
import { SOLFX_CORE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INITIALIZE_USER_ACCOUNT_DISCRIMINATOR = new Uint8Array([
  131, 248, 61, 211, 152, 205, 122, 238,
]);

export function getInitializeUserAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_USER_ACCOUNT_DISCRIMINATOR,
  );
}

export type InitializeUserAccountInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountProtocol extends string | AccountMeta<string> = string,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountProtocol extends string
        ? ReadonlyAccount<TAccountProtocol>
        : TAccountProtocol,
      TAccountUserAccount extends string
        ? WritableAccount<TAccountUserAccount>
        : TAccountUserAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeUserAccountInstructionData = {
  discriminator: ReadonlyUint8Array;
  referrer: Address;
};

export type InitializeUserAccountInstructionDataArgs = { referrer: Address };

export function getInitializeUserAccountInstructionDataEncoder(): FixedSizeEncoder<InitializeUserAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["referrer", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_USER_ACCOUNT_DISCRIMINATOR,
    }),
  );
}

export function getInitializeUserAccountInstructionDataDecoder(): FixedSizeDecoder<InitializeUserAccountInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["referrer", getAddressDecoder()],
  ]);
}

export function getInitializeUserAccountInstructionDataCodec(): FixedSizeCodec<
  InitializeUserAccountInstructionDataArgs,
  InitializeUserAccountInstructionData
> {
  return combineCodec(
    getInitializeUserAccountInstructionDataEncoder(),
    getInitializeUserAccountInstructionDataDecoder(),
  );
}

export type InitializeUserAccountAsyncInput<
  TAccountAuthority extends string = string,
  TAccountProtocol extends string = string,
  TAccountUserAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  protocol?: Address<TAccountProtocol>;
  userAccount?: Address<TAccountUserAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  referrer: InitializeUserAccountInstructionDataArgs["referrer"];
};

export async function getInitializeUserAccountInstructionAsync<
  TAccountAuthority extends string,
  TAccountProtocol extends string,
  TAccountUserAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: InitializeUserAccountAsyncInput<
    TAccountAuthority,
    TAccountProtocol,
    TAccountUserAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeUserAccountInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountProtocol,
    TAccountUserAccount,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    protocol: { value: input.protocol ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocol.value) {
    accounts.protocol.value = await findProtocolPda();
  }
  if (!accounts.userAccount.value) {
    accounts.userAccount.value = await findUserAccountPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeUserAccountInstructionDataEncoder().encode(
      args as InitializeUserAccountInstructionDataArgs,
    ),
    programAddress,
  } as InitializeUserAccountInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountProtocol,
    TAccountUserAccount,
    TAccountSystemProgram
  >);
}

export type InitializeUserAccountInput<
  TAccountAuthority extends string = string,
  TAccountProtocol extends string = string,
  TAccountUserAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  protocol: Address<TAccountProtocol>;
  userAccount: Address<TAccountUserAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  referrer: InitializeUserAccountInstructionDataArgs["referrer"];
};

export function getInitializeUserAccountInstruction<
  TAccountAuthority extends string,
  TAccountProtocol extends string,
  TAccountUserAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: InitializeUserAccountInput<
    TAccountAuthority,
    TAccountProtocol,
    TAccountUserAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeUserAccountInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountProtocol,
  TAccountUserAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    protocol: { value: input.protocol ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeUserAccountInstructionDataEncoder().encode(
      args as InitializeUserAccountInstructionDataArgs,
    ),
    programAddress,
  } as InitializeUserAccountInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountProtocol,
    TAccountUserAccount,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeUserAccountInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    protocol: TAccountMetas[1];
    userAccount: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitializeUserAccountInstructionData;
};

export function parseInitializeUserAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeUserAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      protocol: getNextAccount(),
      userAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeUserAccountInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
