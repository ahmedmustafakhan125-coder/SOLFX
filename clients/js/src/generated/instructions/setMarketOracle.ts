/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findProtocolPda } from "../pdas";
import { SOLFX_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_MARKET_ORACLE_DISCRIMINATOR = new Uint8Array([
  181, 227, 15, 65, 140, 150, 146, 2,
]);

export function getSetMarketOracleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_MARKET_ORACLE_DISCRIMINATOR,
  );
}

export type SetMarketOracleInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountProtocol extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountProtocol extends string
        ? ReadonlyAccount<TAccountProtocol>
        : TAccountProtocol,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      ...TRemainingAccounts,
    ]
  >;

export type SetMarketOracleInstructionData = {
  discriminator: ReadonlyUint8Array;
  expectedCurrent: ReadonlyUint8Array;
  newFeedId: ReadonlyUint8Array;
};

export type SetMarketOracleInstructionDataArgs = {
  expectedCurrent: ReadonlyUint8Array;
  newFeedId: ReadonlyUint8Array;
};

export function getSetMarketOracleInstructionDataEncoder(): FixedSizeEncoder<SetMarketOracleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["expectedCurrent", fixEncoderSize(getBytesEncoder(), 32)],
      ["newFeedId", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: SET_MARKET_ORACLE_DISCRIMINATOR }),
  );
}

export function getSetMarketOracleInstructionDataDecoder(): FixedSizeDecoder<SetMarketOracleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["expectedCurrent", fixDecoderSize(getBytesDecoder(), 32)],
    ["newFeedId", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getSetMarketOracleInstructionDataCodec(): FixedSizeCodec<
  SetMarketOracleInstructionDataArgs,
  SetMarketOracleInstructionData
> {
  return combineCodec(
    getSetMarketOracleInstructionDataEncoder(),
    getSetMarketOracleInstructionDataDecoder(),
  );
}

export type SetMarketOracleAsyncInput<
  TAccountAdmin extends string = string,
  TAccountProtocol extends string = string,
  TAccountMarket extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  protocol?: Address<TAccountProtocol>;
  market: Address<TAccountMarket>;
  expectedCurrent: SetMarketOracleInstructionDataArgs["expectedCurrent"];
  newFeedId: SetMarketOracleInstructionDataArgs["newFeedId"];
};

export async function getSetMarketOracleInstructionAsync<
  TAccountAdmin extends string,
  TAccountProtocol extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: SetMarketOracleAsyncInput<
    TAccountAdmin,
    TAccountProtocol,
    TAccountMarket
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetMarketOracleInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProtocol,
    TAccountMarket
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocol.value) {
    accounts.protocol.value = await findProtocolPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.market),
    ],
    data: getSetMarketOracleInstructionDataEncoder().encode(
      args as SetMarketOracleInstructionDataArgs,
    ),
    programAddress,
  } as SetMarketOracleInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProtocol,
    TAccountMarket
  >);
}

export type SetMarketOracleInput<
  TAccountAdmin extends string = string,
  TAccountProtocol extends string = string,
  TAccountMarket extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  protocol: Address<TAccountProtocol>;
  market: Address<TAccountMarket>;
  expectedCurrent: SetMarketOracleInstructionDataArgs["expectedCurrent"];
  newFeedId: SetMarketOracleInstructionDataArgs["newFeedId"];
};

export function getSetMarketOracleInstruction<
  TAccountAdmin extends string,
  TAccountProtocol extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: SetMarketOracleInput<TAccountAdmin, TAccountProtocol, TAccountMarket>,
  config?: { programAddress?: TProgramAddress },
): SetMarketOracleInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountProtocol,
  TAccountMarket
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.market),
    ],
    data: getSetMarketOracleInstructionDataEncoder().encode(
      args as SetMarketOracleInstructionDataArgs,
    ),
    programAddress,
  } as SetMarketOracleInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProtocol,
    TAccountMarket
  >);
}

export type ParsedSetMarketOracleInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    protocol: TAccountMetas[1];
    market: TAccountMetas[2];
  };
  data: SetMarketOracleInstructionData;
};

export function parseSetMarketOracleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetMarketOracleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      protocol: getNextAccount(),
      market: getNextAccount(),
    },
    data: getSetMarketOracleInstructionDataDecoder().decode(instruction.data),
  };
}
