/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { SOLFX_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CANCEL_TRIGGER_ORDER_DISCRIMINATOR = new Uint8Array([
  144, 84, 67, 39, 27, 25, 202, 141,
]);

export function getCancelTriggerOrderDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_TRIGGER_ORDER_DISCRIMINATOR,
  );
}

export type CancelTriggerOrderInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountTriggerOrder extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountTriggerOrder extends string
        ? WritableAccount<TAccountTriggerOrder>
        : TAccountTriggerOrder,
      ...TRemainingAccounts,
    ]
  >;

export type CancelTriggerOrderInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelTriggerOrderInstructionDataArgs = {};

export function getCancelTriggerOrderInstructionDataEncoder(): FixedSizeEncoder<CancelTriggerOrderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CANCEL_TRIGGER_ORDER_DISCRIMINATOR,
    }),
  );
}

export function getCancelTriggerOrderInstructionDataDecoder(): FixedSizeDecoder<CancelTriggerOrderInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelTriggerOrderInstructionDataCodec(): FixedSizeCodec<
  CancelTriggerOrderInstructionDataArgs,
  CancelTriggerOrderInstructionData
> {
  return combineCodec(
    getCancelTriggerOrderInstructionDataEncoder(),
    getCancelTriggerOrderInstructionDataDecoder(),
  );
}

export type CancelTriggerOrderInput<
  TAccountAuthority extends string = string,
  TAccountTriggerOrder extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  triggerOrder: Address<TAccountTriggerOrder>;
};

export function getCancelTriggerOrderInstruction<
  TAccountAuthority extends string,
  TAccountTriggerOrder extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: CancelTriggerOrderInput<TAccountAuthority, TAccountTriggerOrder>,
  config?: { programAddress?: TProgramAddress },
): CancelTriggerOrderInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountTriggerOrder
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    triggerOrder: { value: input.triggerOrder ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.triggerOrder),
    ],
    data: getCancelTriggerOrderInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelTriggerOrderInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountTriggerOrder
  >);
}

export type ParsedCancelTriggerOrderInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    triggerOrder: TAccountMetas[1];
  };
  data: CancelTriggerOrderInstructionData;
};

export function parseCancelTriggerOrderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelTriggerOrderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), triggerOrder: getNextAccount() },
    data: getCancelTriggerOrderInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
