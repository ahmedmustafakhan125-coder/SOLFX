/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findFeeVaultPda, findProtocolPda } from "../pdas";
import { SOLFX_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const PAY_REFERRAL_DISCRIMINATOR = new Uint8Array([
  223, 35, 124, 185, 198, 19, 229, 163,
]);

export function getPayReferralDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PAY_REFERRAL_DISCRIMINATOR,
  );
}

export type PayReferralInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountReferralAuthority extends string | AccountMeta<string> = string,
  TAccountProtocol extends string | AccountMeta<string> = string,
  TAccountFeeVault extends string | AccountMeta<string> = string,
  TAccountDestination extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountReferralAuthority extends string
        ? ReadonlySignerAccount<TAccountReferralAuthority> &
            AccountSignerMeta<TAccountReferralAuthority>
        : TAccountReferralAuthority,
      TAccountProtocol extends string
        ? WritableAccount<TAccountProtocol>
        : TAccountProtocol,
      TAccountFeeVault extends string
        ? WritableAccount<TAccountFeeVault>
        : TAccountFeeVault,
      TAccountDestination extends string
        ? WritableAccount<TAccountDestination>
        : TAccountDestination,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PayReferralInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type PayReferralInstructionDataArgs = { amount: number | bigint };

export function getPayReferralInstructionDataEncoder(): FixedSizeEncoder<PayReferralInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PAY_REFERRAL_DISCRIMINATOR }),
  );
}

export function getPayReferralInstructionDataDecoder(): FixedSizeDecoder<PayReferralInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getPayReferralInstructionDataCodec(): FixedSizeCodec<
  PayReferralInstructionDataArgs,
  PayReferralInstructionData
> {
  return combineCodec(
    getPayReferralInstructionDataEncoder(),
    getPayReferralInstructionDataDecoder(),
  );
}

export type PayReferralAsyncInput<
  TAccountReferralAuthority extends string = string,
  TAccountProtocol extends string = string,
  TAccountFeeVault extends string = string,
  TAccountDestination extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /**
   * The referral programme's PDA, signing via CPI. Checked against the pubkey the admin
   * registered — a signature from anyone else is not a rebate.
   */
  referralAuthority: TransactionSigner<TAccountReferralAuthority>;
  protocol?: Address<TAccountProtocol>;
  feeVault?: Address<TAccountFeeVault>;
  destination: Address<TAccountDestination>;
  tokenProgram?: Address<TAccountTokenProgram>;
  amount: PayReferralInstructionDataArgs["amount"];
};

export async function getPayReferralInstructionAsync<
  TAccountReferralAuthority extends string,
  TAccountProtocol extends string,
  TAccountFeeVault extends string,
  TAccountDestination extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: PayReferralAsyncInput<
    TAccountReferralAuthority,
    TAccountProtocol,
    TAccountFeeVault,
    TAccountDestination,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PayReferralInstruction<
    TProgramAddress,
    TAccountReferralAuthority,
    TAccountProtocol,
    TAccountFeeVault,
    TAccountDestination,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    referralAuthority: {
      value: input.referralAuthority ?? null,
      isWritable: false,
    },
    protocol: { value: input.protocol ?? null, isWritable: true },
    feeVault: { value: input.feeVault ?? null, isWritable: true },
    destination: { value: input.destination ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocol.value) {
    accounts.protocol.value = await findProtocolPda();
  }
  if (!accounts.feeVault.value) {
    accounts.feeVault.value = await findFeeVaultPda();
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.referralAuthority),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.feeVault),
      getAccountMeta(accounts.destination),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getPayReferralInstructionDataEncoder().encode(
      args as PayReferralInstructionDataArgs,
    ),
    programAddress,
  } as PayReferralInstruction<
    TProgramAddress,
    TAccountReferralAuthority,
    TAccountProtocol,
    TAccountFeeVault,
    TAccountDestination,
    TAccountTokenProgram
  >);
}

export type PayReferralInput<
  TAccountReferralAuthority extends string = string,
  TAccountProtocol extends string = string,
  TAccountFeeVault extends string = string,
  TAccountDestination extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /**
   * The referral programme's PDA, signing via CPI. Checked against the pubkey the admin
   * registered — a signature from anyone else is not a rebate.
   */
  referralAuthority: TransactionSigner<TAccountReferralAuthority>;
  protocol: Address<TAccountProtocol>;
  feeVault: Address<TAccountFeeVault>;
  destination: Address<TAccountDestination>;
  tokenProgram?: Address<TAccountTokenProgram>;
  amount: PayReferralInstructionDataArgs["amount"];
};

export function getPayReferralInstruction<
  TAccountReferralAuthority extends string,
  TAccountProtocol extends string,
  TAccountFeeVault extends string,
  TAccountDestination extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: PayReferralInput<
    TAccountReferralAuthority,
    TAccountProtocol,
    TAccountFeeVault,
    TAccountDestination,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PayReferralInstruction<
  TProgramAddress,
  TAccountReferralAuthority,
  TAccountProtocol,
  TAccountFeeVault,
  TAccountDestination,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    referralAuthority: {
      value: input.referralAuthority ?? null,
      isWritable: false,
    },
    protocol: { value: input.protocol ?? null, isWritable: true },
    feeVault: { value: input.feeVault ?? null, isWritable: true },
    destination: { value: input.destination ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.referralAuthority),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.feeVault),
      getAccountMeta(accounts.destination),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getPayReferralInstructionDataEncoder().encode(
      args as PayReferralInstructionDataArgs,
    ),
    programAddress,
  } as PayReferralInstruction<
    TProgramAddress,
    TAccountReferralAuthority,
    TAccountProtocol,
    TAccountFeeVault,
    TAccountDestination,
    TAccountTokenProgram
  >);
}

export type ParsedPayReferralInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The referral programme's PDA, signing via CPI. Checked against the pubkey the admin
     * registered — a signature from anyone else is not a rebate.
     */
    referralAuthority: TAccountMetas[0];
    protocol: TAccountMetas[1];
    feeVault: TAccountMetas[2];
    destination: TAccountMetas[3];
    tokenProgram: TAccountMetas[4];
  };
  data: PayReferralInstructionData;
};

export function parsePayReferralInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPayReferralInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      referralAuthority: getNextAccount(),
      protocol: getNextAccount(),
      feeVault: getNextAccount(),
      destination: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getPayReferralInstructionDataDecoder().decode(instruction.data),
  };
}
