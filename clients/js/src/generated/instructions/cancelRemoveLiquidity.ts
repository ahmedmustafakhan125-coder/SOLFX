/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findLpPoolPda, findWithdrawRequestPda } from "../pdas";
import { SOLFX_CORE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CANCEL_REMOVE_LIQUIDITY_DISCRIMINATOR = new Uint8Array([
  145, 195, 249, 35, 234, 249, 23, 181,
]);

export function getCancelRemoveLiquidityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_REMOVE_LIQUIDITY_DISCRIMINATOR,
  );
}

export type CancelRemoveLiquidityInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountLpPool extends string | AccountMeta<string> = string,
  TAccountWithdrawRequest extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProvider extends string
        ? WritableSignerAccount<TAccountProvider> &
            AccountSignerMeta<TAccountProvider>
        : TAccountProvider,
      TAccountLpPool extends string
        ? WritableAccount<TAccountLpPool>
        : TAccountLpPool,
      TAccountWithdrawRequest extends string
        ? WritableAccount<TAccountWithdrawRequest>
        : TAccountWithdrawRequest,
      TAccountAuthority extends string
        ? ReadonlyAccount<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type CancelRemoveLiquidityInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelRemoveLiquidityInstructionDataArgs = {};

export function getCancelRemoveLiquidityInstructionDataEncoder(): FixedSizeEncoder<CancelRemoveLiquidityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CANCEL_REMOVE_LIQUIDITY_DISCRIMINATOR,
    }),
  );
}

export function getCancelRemoveLiquidityInstructionDataDecoder(): FixedSizeDecoder<CancelRemoveLiquidityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelRemoveLiquidityInstructionDataCodec(): FixedSizeCodec<
  CancelRemoveLiquidityInstructionDataArgs,
  CancelRemoveLiquidityInstructionData
> {
  return combineCodec(
    getCancelRemoveLiquidityInstructionDataEncoder(),
    getCancelRemoveLiquidityInstructionDataDecoder(),
  );
}

export type CancelRemoveLiquidityAsyncInput<
  TAccountProvider extends string = string,
  TAccountLpPool extends string = string,
  TAccountWithdrawRequest extends string = string,
  TAccountAuthority extends string = string,
> = {
  provider: TransactionSigner<TAccountProvider>;
  lpPool?: Address<TAccountLpPool>;
  withdrawRequest?: Address<TAccountWithdrawRequest>;
  authority: Address<TAccountAuthority>;
};

export async function getCancelRemoveLiquidityInstructionAsync<
  TAccountProvider extends string,
  TAccountLpPool extends string,
  TAccountWithdrawRequest extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: CancelRemoveLiquidityAsyncInput<
    TAccountProvider,
    TAccountLpPool,
    TAccountWithdrawRequest,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelRemoveLiquidityInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountLpPool,
    TAccountWithdrawRequest,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    provider: { value: input.provider ?? null, isWritable: true },
    lpPool: { value: input.lpPool ?? null, isWritable: true },
    withdrawRequest: { value: input.withdrawRequest ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.lpPool.value) {
    accounts.lpPool.value = await findLpPoolPda();
  }
  if (!accounts.withdrawRequest.value) {
    accounts.withdrawRequest.value = await findWithdrawRequestPda({
      provider: expectAddress(accounts.provider.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.provider),
      getAccountMeta(accounts.lpPool),
      getAccountMeta(accounts.withdrawRequest),
      getAccountMeta(accounts.authority),
    ],
    data: getCancelRemoveLiquidityInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelRemoveLiquidityInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountLpPool,
    TAccountWithdrawRequest,
    TAccountAuthority
  >);
}

export type CancelRemoveLiquidityInput<
  TAccountProvider extends string = string,
  TAccountLpPool extends string = string,
  TAccountWithdrawRequest extends string = string,
  TAccountAuthority extends string = string,
> = {
  provider: TransactionSigner<TAccountProvider>;
  lpPool: Address<TAccountLpPool>;
  withdrawRequest: Address<TAccountWithdrawRequest>;
  authority: Address<TAccountAuthority>;
};

export function getCancelRemoveLiquidityInstruction<
  TAccountProvider extends string,
  TAccountLpPool extends string,
  TAccountWithdrawRequest extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: CancelRemoveLiquidityInput<
    TAccountProvider,
    TAccountLpPool,
    TAccountWithdrawRequest,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): CancelRemoveLiquidityInstruction<
  TProgramAddress,
  TAccountProvider,
  TAccountLpPool,
  TAccountWithdrawRequest,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    provider: { value: input.provider ?? null, isWritable: true },
    lpPool: { value: input.lpPool ?? null, isWritable: true },
    withdrawRequest: { value: input.withdrawRequest ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.provider),
      getAccountMeta(accounts.lpPool),
      getAccountMeta(accounts.withdrawRequest),
      getAccountMeta(accounts.authority),
    ],
    data: getCancelRemoveLiquidityInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelRemoveLiquidityInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountLpPool,
    TAccountWithdrawRequest,
    TAccountAuthority
  >);
}

export type ParsedCancelRemoveLiquidityInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    provider: TAccountMetas[0];
    lpPool: TAccountMetas[1];
    withdrawRequest: TAccountMetas[2];
    authority: TAccountMetas[3];
  };
  data: CancelRemoveLiquidityInstructionData;
};

export function parseCancelRemoveLiquidityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelRemoveLiquidityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      provider: getNextAccount(),
      lpPool: getNextAccount(),
      withdrawRequest: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getCancelRemoveLiquidityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
