/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findProtocolPda } from "../pdas";
import { SOLFX_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";
import {
  getMarketStatusDecoder,
  getMarketStatusEncoder,
  type MarketStatus,
  type MarketStatusArgs,
} from "../types";

export const SET_MARKET_STATUS_DISCRIMINATOR = new Uint8Array([
  101, 175, 83, 107, 200, 141, 155, 182,
]);

export function getSetMarketStatusDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_MARKET_STATUS_DISCRIMINATOR,
  );
}

export type SetMarketStatusInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountProtocol extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountProtocol extends string
        ? ReadonlyAccount<TAccountProtocol>
        : TAccountProtocol,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      ...TRemainingAccounts,
    ]
  >;

export type SetMarketStatusInstructionData = {
  discriminator: ReadonlyUint8Array;
  newStatus: MarketStatus;
};

export type SetMarketStatusInstructionDataArgs = {
  newStatus: MarketStatusArgs;
};

export function getSetMarketStatusInstructionDataEncoder(): FixedSizeEncoder<SetMarketStatusInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newStatus", getMarketStatusEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_MARKET_STATUS_DISCRIMINATOR }),
  );
}

export function getSetMarketStatusInstructionDataDecoder(): FixedSizeDecoder<SetMarketStatusInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newStatus", getMarketStatusDecoder()],
  ]);
}

export function getSetMarketStatusInstructionDataCodec(): FixedSizeCodec<
  SetMarketStatusInstructionDataArgs,
  SetMarketStatusInstructionData
> {
  return combineCodec(
    getSetMarketStatusInstructionDataEncoder(),
    getSetMarketStatusInstructionDataDecoder(),
  );
}

export type SetMarketStatusAsyncInput<
  TAccountAdmin extends string = string,
  TAccountProtocol extends string = string,
  TAccountMarket extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  protocol?: Address<TAccountProtocol>;
  market: Address<TAccountMarket>;
  newStatus: SetMarketStatusInstructionDataArgs["newStatus"];
};

export async function getSetMarketStatusInstructionAsync<
  TAccountAdmin extends string,
  TAccountProtocol extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: SetMarketStatusAsyncInput<
    TAccountAdmin,
    TAccountProtocol,
    TAccountMarket
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetMarketStatusInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProtocol,
    TAccountMarket
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocol.value) {
    accounts.protocol.value = await findProtocolPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.market),
    ],
    data: getSetMarketStatusInstructionDataEncoder().encode(
      args as SetMarketStatusInstructionDataArgs,
    ),
    programAddress,
  } as SetMarketStatusInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProtocol,
    TAccountMarket
  >);
}

export type SetMarketStatusInput<
  TAccountAdmin extends string = string,
  TAccountProtocol extends string = string,
  TAccountMarket extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  protocol: Address<TAccountProtocol>;
  market: Address<TAccountMarket>;
  newStatus: SetMarketStatusInstructionDataArgs["newStatus"];
};

export function getSetMarketStatusInstruction<
  TAccountAdmin extends string,
  TAccountProtocol extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: SetMarketStatusInput<TAccountAdmin, TAccountProtocol, TAccountMarket>,
  config?: { programAddress?: TProgramAddress },
): SetMarketStatusInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountProtocol,
  TAccountMarket
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.market),
    ],
    data: getSetMarketStatusInstructionDataEncoder().encode(
      args as SetMarketStatusInstructionDataArgs,
    ),
    programAddress,
  } as SetMarketStatusInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProtocol,
    TAccountMarket
  >);
}

export type ParsedSetMarketStatusInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    protocol: TAccountMetas[1];
    market: TAccountMetas[2];
  };
  data: SetMarketStatusInstructionData;
};

export function parseSetMarketStatusInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetMarketStatusInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      protocol: getNextAccount(),
      market: getNextAccount(),
    },
    data: getSetMarketStatusInstructionDataDecoder().decode(instruction.data),
  };
}
