/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findProtocolPda } from "../pdas";
import { SOLFX_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CRANK_MARKET_SESSION_DISCRIMINATOR = new Uint8Array([
  130, 222, 7, 146, 1, 83, 244, 255,
]);

export function getCrankMarketSessionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CRANK_MARKET_SESSION_DISCRIMINATOR,
  );
}

export type CrankMarketSessionInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountKeeper extends string | AccountMeta<string> = string,
  TAccountProtocol extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountPriceUpdate extends string | AccountMeta<string> = string,
  TAccountSecondaryPriceUpdate extends string | AccountMeta<string> = string,
  TAccountQuoteConversionPriceUpdate extends string | AccountMeta<string> =
    string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountKeeper extends string
        ? ReadonlySignerAccount<TAccountKeeper> &
            AccountSignerMeta<TAccountKeeper>
        : TAccountKeeper,
      TAccountProtocol extends string
        ? ReadonlyAccount<TAccountProtocol>
        : TAccountProtocol,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountPriceUpdate extends string
        ? ReadonlyAccount<TAccountPriceUpdate>
        : TAccountPriceUpdate,
      TAccountSecondaryPriceUpdate extends string
        ? ReadonlyAccount<TAccountSecondaryPriceUpdate>
        : TAccountSecondaryPriceUpdate,
      TAccountQuoteConversionPriceUpdate extends string
        ? ReadonlyAccount<TAccountQuoteConversionPriceUpdate>
        : TAccountQuoteConversionPriceUpdate,
      ...TRemainingAccounts,
    ]
  >;

export type CrankMarketSessionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CrankMarketSessionInstructionDataArgs = {};

export function getCrankMarketSessionInstructionDataEncoder(): FixedSizeEncoder<CrankMarketSessionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CRANK_MARKET_SESSION_DISCRIMINATOR,
    }),
  );
}

export function getCrankMarketSessionInstructionDataDecoder(): FixedSizeDecoder<CrankMarketSessionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCrankMarketSessionInstructionDataCodec(): FixedSizeCodec<
  CrankMarketSessionInstructionDataArgs,
  CrankMarketSessionInstructionData
> {
  return combineCodec(
    getCrankMarketSessionInstructionDataEncoder(),
    getCrankMarketSessionInstructionDataDecoder(),
  );
}

export type CrankMarketSessionAsyncInput<
  TAccountKeeper extends string = string,
  TAccountProtocol extends string = string,
  TAccountMarket extends string = string,
  TAccountPriceUpdate extends string = string,
  TAccountSecondaryPriceUpdate extends string = string,
  TAccountQuoteConversionPriceUpdate extends string = string,
> = {
  /**
   * Permissionless — see `crank_market_price`. A market whose session only its operator
   * can advance is a market that stays open when the operator is asleep.
   */
  keeper: TransactionSigner<TAccountKeeper>;
  protocol?: Address<TAccountProtocol>;
  market: Address<TAccountMarket>;
  /**
   * Optional. When supplied, the feed's freshness is the primary signal; without it the
   * calendar alone decides.
   *
   * Optional rather than required because the crank must keep working when the feed has
   * stopped entirely — which is precisely the case it exists to handle. A required
   * account would make the market unclosable exactly when it most needs closing.
   */
  priceUpdate?: Address<TAccountPriceUpdate>;
  secondaryPriceUpdate?: Address<TAccountSecondaryPriceUpdate>;
  quoteConversionPriceUpdate?: Address<TAccountQuoteConversionPriceUpdate>;
};

export async function getCrankMarketSessionInstructionAsync<
  TAccountKeeper extends string,
  TAccountProtocol extends string,
  TAccountMarket extends string,
  TAccountPriceUpdate extends string,
  TAccountSecondaryPriceUpdate extends string,
  TAccountQuoteConversionPriceUpdate extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: CrankMarketSessionAsyncInput<
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket,
    TAccountPriceUpdate,
    TAccountSecondaryPriceUpdate,
    TAccountQuoteConversionPriceUpdate
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CrankMarketSessionInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket,
    TAccountPriceUpdate,
    TAccountSecondaryPriceUpdate,
    TAccountQuoteConversionPriceUpdate
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    keeper: { value: input.keeper ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    priceUpdate: { value: input.priceUpdate ?? null, isWritable: false },
    secondaryPriceUpdate: {
      value: input.secondaryPriceUpdate ?? null,
      isWritable: false,
    },
    quoteConversionPriceUpdate: {
      value: input.quoteConversionPriceUpdate ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.protocol.value) {
    accounts.protocol.value = await findProtocolPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.keeper),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.priceUpdate),
      getAccountMeta(accounts.secondaryPriceUpdate),
      getAccountMeta(accounts.quoteConversionPriceUpdate),
    ],
    data: getCrankMarketSessionInstructionDataEncoder().encode({}),
    programAddress,
  } as CrankMarketSessionInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket,
    TAccountPriceUpdate,
    TAccountSecondaryPriceUpdate,
    TAccountQuoteConversionPriceUpdate
  >);
}

export type CrankMarketSessionInput<
  TAccountKeeper extends string = string,
  TAccountProtocol extends string = string,
  TAccountMarket extends string = string,
  TAccountPriceUpdate extends string = string,
  TAccountSecondaryPriceUpdate extends string = string,
  TAccountQuoteConversionPriceUpdate extends string = string,
> = {
  /**
   * Permissionless — see `crank_market_price`. A market whose session only its operator
   * can advance is a market that stays open when the operator is asleep.
   */
  keeper: TransactionSigner<TAccountKeeper>;
  protocol: Address<TAccountProtocol>;
  market: Address<TAccountMarket>;
  /**
   * Optional. When supplied, the feed's freshness is the primary signal; without it the
   * calendar alone decides.
   *
   * Optional rather than required because the crank must keep working when the feed has
   * stopped entirely — which is precisely the case it exists to handle. A required
   * account would make the market unclosable exactly when it most needs closing.
   */
  priceUpdate?: Address<TAccountPriceUpdate>;
  secondaryPriceUpdate?: Address<TAccountSecondaryPriceUpdate>;
  quoteConversionPriceUpdate?: Address<TAccountQuoteConversionPriceUpdate>;
};

export function getCrankMarketSessionInstruction<
  TAccountKeeper extends string,
  TAccountProtocol extends string,
  TAccountMarket extends string,
  TAccountPriceUpdate extends string,
  TAccountSecondaryPriceUpdate extends string,
  TAccountQuoteConversionPriceUpdate extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: CrankMarketSessionInput<
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket,
    TAccountPriceUpdate,
    TAccountSecondaryPriceUpdate,
    TAccountQuoteConversionPriceUpdate
  >,
  config?: { programAddress?: TProgramAddress },
): CrankMarketSessionInstruction<
  TProgramAddress,
  TAccountKeeper,
  TAccountProtocol,
  TAccountMarket,
  TAccountPriceUpdate,
  TAccountSecondaryPriceUpdate,
  TAccountQuoteConversionPriceUpdate
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    keeper: { value: input.keeper ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    priceUpdate: { value: input.priceUpdate ?? null, isWritable: false },
    secondaryPriceUpdate: {
      value: input.secondaryPriceUpdate ?? null,
      isWritable: false,
    },
    quoteConversionPriceUpdate: {
      value: input.quoteConversionPriceUpdate ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.keeper),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.priceUpdate),
      getAccountMeta(accounts.secondaryPriceUpdate),
      getAccountMeta(accounts.quoteConversionPriceUpdate),
    ],
    data: getCrankMarketSessionInstructionDataEncoder().encode({}),
    programAddress,
  } as CrankMarketSessionInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket,
    TAccountPriceUpdate,
    TAccountSecondaryPriceUpdate,
    TAccountQuoteConversionPriceUpdate
  >);
}

export type ParsedCrankMarketSessionInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Permissionless — see `crank_market_price`. A market whose session only its operator
     * can advance is a market that stays open when the operator is asleep.
     */
    keeper: TAccountMetas[0];
    protocol: TAccountMetas[1];
    market: TAccountMetas[2];
    /**
     * Optional. When supplied, the feed's freshness is the primary signal; without it the
     * calendar alone decides.
     *
     * Optional rather than required because the crank must keep working when the feed has
     * stopped entirely — which is precisely the case it exists to handle. A required
     * account would make the market unclosable exactly when it most needs closing.
     */
    priceUpdate?: TAccountMetas[3] | undefined;
    secondaryPriceUpdate?: TAccountMetas[4] | undefined;
    quoteConversionPriceUpdate?: TAccountMetas[5] | undefined;
  };
  data: CrankMarketSessionInstructionData;
};

export function parseCrankMarketSessionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCrankMarketSessionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === SOLFX_CORE_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      keeper: getNextAccount(),
      protocol: getNextAccount(),
      market: getNextAccount(),
      priceUpdate: getNextOptionalAccount(),
      secondaryPriceUpdate: getNextOptionalAccount(),
      quoteConversionPriceUpdate: getNextOptionalAccount(),
    },
    data: getCrankMarketSessionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
