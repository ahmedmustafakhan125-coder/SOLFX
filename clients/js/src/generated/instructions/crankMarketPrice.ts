/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findProtocolPda } from "../pdas";
import { SOLFX_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CRANK_MARKET_PRICE_DISCRIMINATOR = new Uint8Array([
  6, 231, 204, 2, 53, 65, 40, 65,
]);

export function getCrankMarketPriceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CRANK_MARKET_PRICE_DISCRIMINATOR,
  );
}

export type CrankMarketPriceInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountKeeper extends string | AccountMeta<string> = string,
  TAccountProtocol extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountPriceUpdate extends string | AccountMeta<string> = string,
  TAccountSecondaryPriceUpdate extends string | AccountMeta<string> = string,
  TAccountQuoteConversionPriceUpdate extends string | AccountMeta<string> =
    string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountKeeper extends string
        ? ReadonlySignerAccount<TAccountKeeper> &
            AccountSignerMeta<TAccountKeeper>
        : TAccountKeeper,
      TAccountProtocol extends string
        ? ReadonlyAccount<TAccountProtocol>
        : TAccountProtocol,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountPriceUpdate extends string
        ? ReadonlyAccount<TAccountPriceUpdate>
        : TAccountPriceUpdate,
      TAccountSecondaryPriceUpdate extends string
        ? ReadonlyAccount<TAccountSecondaryPriceUpdate>
        : TAccountSecondaryPriceUpdate,
      TAccountQuoteConversionPriceUpdate extends string
        ? ReadonlyAccount<TAccountQuoteConversionPriceUpdate>
        : TAccountQuoteConversionPriceUpdate,
      ...TRemainingAccounts,
    ]
  >;

export type CrankMarketPriceInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CrankMarketPriceInstructionDataArgs = {};

export function getCrankMarketPriceInstructionDataEncoder(): FixedSizeEncoder<CrankMarketPriceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CRANK_MARKET_PRICE_DISCRIMINATOR }),
  );
}

export function getCrankMarketPriceInstructionDataDecoder(): FixedSizeDecoder<CrankMarketPriceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCrankMarketPriceInstructionDataCodec(): FixedSizeCodec<
  CrankMarketPriceInstructionDataArgs,
  CrankMarketPriceInstructionData
> {
  return combineCodec(
    getCrankMarketPriceInstructionDataEncoder(),
    getCrankMarketPriceInstructionDataDecoder(),
  );
}

export type CrankMarketPriceAsyncInput<
  TAccountKeeper extends string = string,
  TAccountProtocol extends string = string,
  TAccountMarket extends string = string,
  TAccountPriceUpdate extends string = string,
  TAccountSecondaryPriceUpdate extends string = string,
  TAccountQuoteConversionPriceUpdate extends string = string,
> = {
  /**
   * Permissionless. Anyone may refresh a market's observed price — there is nothing to
   * steal and a market whose price only its operator can record is a market with a single
   * point of failure.
   */
  keeper: TransactionSigner<TAccountKeeper>;
  protocol?: Address<TAccountProtocol>;
  market: Address<TAccountMarket>;
  /**
   * Primary Pyth pull-oracle update. Posted by the caller in the same transaction
   * (correction C-5) — the account is verified and read, never trusted.
   */
  priceUpdate: Address<TAccountPriceUpdate>;
  /** Quote leg, for synthetic markets. `None` otherwise. */
  secondaryPriceUpdate?: Address<TAccountSecondaryPriceUpdate>;
  /** Quote-currency conversion feed, for non-USD-quoted markets (C-3). `None` otherwise. */
  quoteConversionPriceUpdate?: Address<TAccountQuoteConversionPriceUpdate>;
};

export async function getCrankMarketPriceInstructionAsync<
  TAccountKeeper extends string,
  TAccountProtocol extends string,
  TAccountMarket extends string,
  TAccountPriceUpdate extends string,
  TAccountSecondaryPriceUpdate extends string,
  TAccountQuoteConversionPriceUpdate extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: CrankMarketPriceAsyncInput<
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket,
    TAccountPriceUpdate,
    TAccountSecondaryPriceUpdate,
    TAccountQuoteConversionPriceUpdate
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CrankMarketPriceInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket,
    TAccountPriceUpdate,
    TAccountSecondaryPriceUpdate,
    TAccountQuoteConversionPriceUpdate
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    keeper: { value: input.keeper ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    priceUpdate: { value: input.priceUpdate ?? null, isWritable: false },
    secondaryPriceUpdate: {
      value: input.secondaryPriceUpdate ?? null,
      isWritable: false,
    },
    quoteConversionPriceUpdate: {
      value: input.quoteConversionPriceUpdate ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.protocol.value) {
    accounts.protocol.value = await findProtocolPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.keeper),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.priceUpdate),
      getAccountMeta(accounts.secondaryPriceUpdate),
      getAccountMeta(accounts.quoteConversionPriceUpdate),
    ],
    data: getCrankMarketPriceInstructionDataEncoder().encode({}),
    programAddress,
  } as CrankMarketPriceInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket,
    TAccountPriceUpdate,
    TAccountSecondaryPriceUpdate,
    TAccountQuoteConversionPriceUpdate
  >);
}

export type CrankMarketPriceInput<
  TAccountKeeper extends string = string,
  TAccountProtocol extends string = string,
  TAccountMarket extends string = string,
  TAccountPriceUpdate extends string = string,
  TAccountSecondaryPriceUpdate extends string = string,
  TAccountQuoteConversionPriceUpdate extends string = string,
> = {
  /**
   * Permissionless. Anyone may refresh a market's observed price — there is nothing to
   * steal and a market whose price only its operator can record is a market with a single
   * point of failure.
   */
  keeper: TransactionSigner<TAccountKeeper>;
  protocol: Address<TAccountProtocol>;
  market: Address<TAccountMarket>;
  /**
   * Primary Pyth pull-oracle update. Posted by the caller in the same transaction
   * (correction C-5) — the account is verified and read, never trusted.
   */
  priceUpdate: Address<TAccountPriceUpdate>;
  /** Quote leg, for synthetic markets. `None` otherwise. */
  secondaryPriceUpdate?: Address<TAccountSecondaryPriceUpdate>;
  /** Quote-currency conversion feed, for non-USD-quoted markets (C-3). `None` otherwise. */
  quoteConversionPriceUpdate?: Address<TAccountQuoteConversionPriceUpdate>;
};

export function getCrankMarketPriceInstruction<
  TAccountKeeper extends string,
  TAccountProtocol extends string,
  TAccountMarket extends string,
  TAccountPriceUpdate extends string,
  TAccountSecondaryPriceUpdate extends string,
  TAccountQuoteConversionPriceUpdate extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: CrankMarketPriceInput<
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket,
    TAccountPriceUpdate,
    TAccountSecondaryPriceUpdate,
    TAccountQuoteConversionPriceUpdate
  >,
  config?: { programAddress?: TProgramAddress },
): CrankMarketPriceInstruction<
  TProgramAddress,
  TAccountKeeper,
  TAccountProtocol,
  TAccountMarket,
  TAccountPriceUpdate,
  TAccountSecondaryPriceUpdate,
  TAccountQuoteConversionPriceUpdate
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    keeper: { value: input.keeper ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    priceUpdate: { value: input.priceUpdate ?? null, isWritable: false },
    secondaryPriceUpdate: {
      value: input.secondaryPriceUpdate ?? null,
      isWritable: false,
    },
    quoteConversionPriceUpdate: {
      value: input.quoteConversionPriceUpdate ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.keeper),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.priceUpdate),
      getAccountMeta(accounts.secondaryPriceUpdate),
      getAccountMeta(accounts.quoteConversionPriceUpdate),
    ],
    data: getCrankMarketPriceInstructionDataEncoder().encode({}),
    programAddress,
  } as CrankMarketPriceInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket,
    TAccountPriceUpdate,
    TAccountSecondaryPriceUpdate,
    TAccountQuoteConversionPriceUpdate
  >);
}

export type ParsedCrankMarketPriceInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Permissionless. Anyone may refresh a market's observed price — there is nothing to
     * steal and a market whose price only its operator can record is a market with a single
     * point of failure.
     */
    keeper: TAccountMetas[0];
    protocol: TAccountMetas[1];
    market: TAccountMetas[2];
    /**
     * Primary Pyth pull-oracle update. Posted by the caller in the same transaction
     * (correction C-5) — the account is verified and read, never trusted.
     */
    priceUpdate: TAccountMetas[3];
    /** Quote leg, for synthetic markets. `None` otherwise. */
    secondaryPriceUpdate?: TAccountMetas[4] | undefined;
    /** Quote-currency conversion feed, for non-USD-quoted markets (C-3). `None` otherwise. */
    quoteConversionPriceUpdate?: TAccountMetas[5] | undefined;
  };
  data: CrankMarketPriceInstructionData;
};

export function parseCrankMarketPriceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCrankMarketPriceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === SOLFX_CORE_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      keeper: getNextAccount(),
      protocol: getNextAccount(),
      market: getNextAccount(),
      priceUpdate: getNextAccount(),
      secondaryPriceUpdate: getNextOptionalAccount(),
      quoteConversionPriceUpdate: getNextOptionalAccount(),
    },
    data: getCrankMarketPriceInstructionDataDecoder().decode(instruction.data),
  };
}
