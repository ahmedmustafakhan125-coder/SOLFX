/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findProtocolPda } from "../pdas";
import { SOLFX_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const HALT_MARKET_DISCRIMINATOR = new Uint8Array([
  193, 154, 33, 69, 171, 105, 36, 212,
]);

export function getHaltMarketDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(HALT_MARKET_DISCRIMINATOR);
}

export type HaltMarketInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountGuardian extends string | AccountMeta<string> = string,
  TAccountProtocol extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGuardian extends string
        ? ReadonlySignerAccount<TAccountGuardian> &
            AccountSignerMeta<TAccountGuardian>
        : TAccountGuardian,
      TAccountProtocol extends string
        ? ReadonlyAccount<TAccountProtocol>
        : TAccountProtocol,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      ...TRemainingAccounts,
    ]
  >;

export type HaltMarketInstructionData = { discriminator: ReadonlyUint8Array };

export type HaltMarketInstructionDataArgs = {};

export function getHaltMarketInstructionDataEncoder(): FixedSizeEncoder<HaltMarketInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: HALT_MARKET_DISCRIMINATOR }),
  );
}

export function getHaltMarketInstructionDataDecoder(): FixedSizeDecoder<HaltMarketInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getHaltMarketInstructionDataCodec(): FixedSizeCodec<
  HaltMarketInstructionDataArgs,
  HaltMarketInstructionData
> {
  return combineCodec(
    getHaltMarketInstructionDataEncoder(),
    getHaltMarketInstructionDataDecoder(),
  );
}

export type HaltMarketAsyncInput<
  TAccountGuardian extends string = string,
  TAccountProtocol extends string = string,
  TAccountMarket extends string = string,
> = {
  guardian: TransactionSigner<TAccountGuardian>;
  protocol?: Address<TAccountProtocol>;
  market: Address<TAccountMarket>;
};

export async function getHaltMarketInstructionAsync<
  TAccountGuardian extends string,
  TAccountProtocol extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: HaltMarketAsyncInput<
    TAccountGuardian,
    TAccountProtocol,
    TAccountMarket
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  HaltMarketInstruction<
    TProgramAddress,
    TAccountGuardian,
    TAccountProtocol,
    TAccountMarket
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    guardian: { value: input.guardian ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.protocol.value) {
    accounts.protocol.value = await findProtocolPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.guardian),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.market),
    ],
    data: getHaltMarketInstructionDataEncoder().encode({}),
    programAddress,
  } as HaltMarketInstruction<
    TProgramAddress,
    TAccountGuardian,
    TAccountProtocol,
    TAccountMarket
  >);
}

export type HaltMarketInput<
  TAccountGuardian extends string = string,
  TAccountProtocol extends string = string,
  TAccountMarket extends string = string,
> = {
  guardian: TransactionSigner<TAccountGuardian>;
  protocol: Address<TAccountProtocol>;
  market: Address<TAccountMarket>;
};

export function getHaltMarketInstruction<
  TAccountGuardian extends string,
  TAccountProtocol extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: HaltMarketInput<TAccountGuardian, TAccountProtocol, TAccountMarket>,
  config?: { programAddress?: TProgramAddress },
): HaltMarketInstruction<
  TProgramAddress,
  TAccountGuardian,
  TAccountProtocol,
  TAccountMarket
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    guardian: { value: input.guardian ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.guardian),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.market),
    ],
    data: getHaltMarketInstructionDataEncoder().encode({}),
    programAddress,
  } as HaltMarketInstruction<
    TProgramAddress,
    TAccountGuardian,
    TAccountProtocol,
    TAccountMarket
  >);
}

export type ParsedHaltMarketInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    guardian: TAccountMetas[0];
    protocol: TAccountMetas[1];
    market: TAccountMetas[2];
  };
  data: HaltMarketInstructionData;
};

export function parseHaltMarketInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedHaltMarketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      guardian: getNextAccount(),
      protocol: getNextAccount(),
      market: getNextAccount(),
    },
    data: getHaltMarketInstructionDataDecoder().decode(instruction.data),
  };
}
