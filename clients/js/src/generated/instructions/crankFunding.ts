/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findProtocolPda } from "../pdas";
import { SOLFX_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CRANK_FUNDING_DISCRIMINATOR = new Uint8Array([
  83, 30, 120, 80, 177, 60, 27, 119,
]);

export function getCrankFundingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CRANK_FUNDING_DISCRIMINATOR,
  );
}

export type CrankFundingInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountKeeper extends string | AccountMeta<string> = string,
  TAccountProtocol extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountKeeper extends string
        ? ReadonlySignerAccount<TAccountKeeper> &
            AccountSignerMeta<TAccountKeeper>
        : TAccountKeeper,
      TAccountProtocol extends string
        ? ReadonlyAccount<TAccountProtocol>
        : TAccountProtocol,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      ...TRemainingAccounts,
    ]
  >;

export type CrankFundingInstructionData = { discriminator: ReadonlyUint8Array };

export type CrankFundingInstructionDataArgs = {};

export function getCrankFundingInstructionDataEncoder(): FixedSizeEncoder<CrankFundingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CRANK_FUNDING_DISCRIMINATOR }),
  );
}

export function getCrankFundingInstructionDataDecoder(): FixedSizeDecoder<CrankFundingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCrankFundingInstructionDataCodec(): FixedSizeCodec<
  CrankFundingInstructionDataArgs,
  CrankFundingInstructionData
> {
  return combineCodec(
    getCrankFundingInstructionDataEncoder(),
    getCrankFundingInstructionDataDecoder(),
  );
}

export type CrankFundingAsyncInput<
  TAccountKeeper extends string = string,
  TAccountProtocol extends string = string,
  TAccountMarket extends string = string,
> = {
  /**
   * Permissionless. Funding that only the operator can advance is funding the operator
   * can choose not to advance when it would cost them.
   */
  keeper: TransactionSigner<TAccountKeeper>;
  protocol?: Address<TAccountProtocol>;
  market: Address<TAccountMarket>;
};

export async function getCrankFundingInstructionAsync<
  TAccountKeeper extends string,
  TAccountProtocol extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: CrankFundingAsyncInput<
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CrankFundingInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    keeper: { value: input.keeper ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.protocol.value) {
    accounts.protocol.value = await findProtocolPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.keeper),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.market),
    ],
    data: getCrankFundingInstructionDataEncoder().encode({}),
    programAddress,
  } as CrankFundingInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket
  >);
}

export type CrankFundingInput<
  TAccountKeeper extends string = string,
  TAccountProtocol extends string = string,
  TAccountMarket extends string = string,
> = {
  /**
   * Permissionless. Funding that only the operator can advance is funding the operator
   * can choose not to advance when it would cost them.
   */
  keeper: TransactionSigner<TAccountKeeper>;
  protocol: Address<TAccountProtocol>;
  market: Address<TAccountMarket>;
};

export function getCrankFundingInstruction<
  TAccountKeeper extends string,
  TAccountProtocol extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: CrankFundingInput<TAccountKeeper, TAccountProtocol, TAccountMarket>,
  config?: { programAddress?: TProgramAddress },
): CrankFundingInstruction<
  TProgramAddress,
  TAccountKeeper,
  TAccountProtocol,
  TAccountMarket
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    keeper: { value: input.keeper ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.keeper),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.market),
    ],
    data: getCrankFundingInstructionDataEncoder().encode({}),
    programAddress,
  } as CrankFundingInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountProtocol,
    TAccountMarket
  >);
}

export type ParsedCrankFundingInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Permissionless. Funding that only the operator can advance is funding the operator
     * can choose not to advance when it would cost them.
     */
    keeper: TAccountMetas[0];
    protocol: TAccountMetas[1];
    market: TAccountMetas[2];
  };
  data: CrankFundingInstructionData;
};

export function parseCrankFundingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCrankFundingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      keeper: getNextAccount(),
      protocol: getNextAccount(),
      market: getNextAccount(),
    },
    data: getCrankFundingInstructionDataDecoder().decode(instruction.data),
  };
}
