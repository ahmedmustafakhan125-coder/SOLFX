/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findProtocolPda } from "../pdas";
import { SOLFX_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_REFERRAL_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  90, 144, 224, 28, 55, 233, 56, 25,
]);

export function getSetReferralAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_REFERRAL_AUTHORITY_DISCRIMINATOR,
  );
}

export type SetReferralAuthorityInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountProtocol extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountProtocol extends string
        ? WritableAccount<TAccountProtocol>
        : TAccountProtocol,
      ...TRemainingAccounts,
    ]
  >;

export type SetReferralAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
};

export type SetReferralAuthorityInstructionDataArgs = { authority: Address };

export function getSetReferralAuthorityInstructionDataEncoder(): FixedSizeEncoder<SetReferralAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_REFERRAL_AUTHORITY_DISCRIMINATOR,
    }),
  );
}

export function getSetReferralAuthorityInstructionDataDecoder(): FixedSizeDecoder<SetReferralAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
  ]);
}

export function getSetReferralAuthorityInstructionDataCodec(): FixedSizeCodec<
  SetReferralAuthorityInstructionDataArgs,
  SetReferralAuthorityInstructionData
> {
  return combineCodec(
    getSetReferralAuthorityInstructionDataEncoder(),
    getSetReferralAuthorityInstructionDataDecoder(),
  );
}

export type SetReferralAuthorityAsyncInput<
  TAccountAdmin extends string = string,
  TAccountProtocol extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  protocol?: Address<TAccountProtocol>;
  authority: SetReferralAuthorityInstructionDataArgs["authority"];
};

export async function getSetReferralAuthorityInstructionAsync<
  TAccountAdmin extends string,
  TAccountProtocol extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: SetReferralAuthorityAsyncInput<TAccountAdmin, TAccountProtocol>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetReferralAuthorityInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProtocol
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocol.value) {
    accounts.protocol.value = await findProtocolPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.protocol),
    ],
    data: getSetReferralAuthorityInstructionDataEncoder().encode(
      args as SetReferralAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as SetReferralAuthorityInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProtocol
  >);
}

export type SetReferralAuthorityInput<
  TAccountAdmin extends string = string,
  TAccountProtocol extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  protocol: Address<TAccountProtocol>;
  authority: SetReferralAuthorityInstructionDataArgs["authority"];
};

export function getSetReferralAuthorityInstruction<
  TAccountAdmin extends string,
  TAccountProtocol extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: SetReferralAuthorityInput<TAccountAdmin, TAccountProtocol>,
  config?: { programAddress?: TProgramAddress },
): SetReferralAuthorityInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountProtocol
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.protocol),
    ],
    data: getSetReferralAuthorityInstructionDataEncoder().encode(
      args as SetReferralAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as SetReferralAuthorityInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProtocol
  >);
}

export type ParsedSetReferralAuthorityInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    protocol: TAccountMetas[1];
  };
  data: SetReferralAuthorityInstructionData;
};

export function parseSetReferralAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetReferralAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), protocol: getNextAccount() },
    data: getSetReferralAuthorityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
