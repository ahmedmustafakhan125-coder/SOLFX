/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findProtocolPda } from "../pdas";
import { SOLFX_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_GUARDIAN_DISCRIMINATOR = new Uint8Array([
  147, 243, 50, 121, 154, 164, 50, 30,
]);

export function getSetGuardianDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_GUARDIAN_DISCRIMINATOR,
  );
}

export type SetGuardianInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountProtocol extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountProtocol extends string
        ? WritableAccount<TAccountProtocol>
        : TAccountProtocol,
      ...TRemainingAccounts,
    ]
  >;

export type SetGuardianInstructionData = {
  discriminator: ReadonlyUint8Array;
  newGuardian: Address;
};

export type SetGuardianInstructionDataArgs = { newGuardian: Address };

export function getSetGuardianInstructionDataEncoder(): FixedSizeEncoder<SetGuardianInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newGuardian", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_GUARDIAN_DISCRIMINATOR }),
  );
}

export function getSetGuardianInstructionDataDecoder(): FixedSizeDecoder<SetGuardianInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newGuardian", getAddressDecoder()],
  ]);
}

export function getSetGuardianInstructionDataCodec(): FixedSizeCodec<
  SetGuardianInstructionDataArgs,
  SetGuardianInstructionData
> {
  return combineCodec(
    getSetGuardianInstructionDataEncoder(),
    getSetGuardianInstructionDataDecoder(),
  );
}

export type SetGuardianAsyncInput<
  TAccountAdmin extends string = string,
  TAccountProtocol extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  protocol?: Address<TAccountProtocol>;
  newGuardian: SetGuardianInstructionDataArgs["newGuardian"];
};

export async function getSetGuardianInstructionAsync<
  TAccountAdmin extends string,
  TAccountProtocol extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: SetGuardianAsyncInput<TAccountAdmin, TAccountProtocol>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetGuardianInstruction<TProgramAddress, TAccountAdmin, TAccountProtocol>
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocol.value) {
    accounts.protocol.value = await findProtocolPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.protocol),
    ],
    data: getSetGuardianInstructionDataEncoder().encode(
      args as SetGuardianInstructionDataArgs,
    ),
    programAddress,
  } as SetGuardianInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProtocol
  >);
}

export type SetGuardianInput<
  TAccountAdmin extends string = string,
  TAccountProtocol extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  protocol: Address<TAccountProtocol>;
  newGuardian: SetGuardianInstructionDataArgs["newGuardian"];
};

export function getSetGuardianInstruction<
  TAccountAdmin extends string,
  TAccountProtocol extends string,
  TProgramAddress extends Address = typeof SOLFX_CORE_PROGRAM_ADDRESS,
>(
  input: SetGuardianInput<TAccountAdmin, TAccountProtocol>,
  config?: { programAddress?: TProgramAddress },
): SetGuardianInstruction<TProgramAddress, TAccountAdmin, TAccountProtocol> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLFX_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    protocol: { value: input.protocol ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.protocol),
    ],
    data: getSetGuardianInstructionDataEncoder().encode(
      args as SetGuardianInstructionDataArgs,
    ),
    programAddress,
  } as SetGuardianInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProtocol
  >);
}

export type ParsedSetGuardianInstruction<
  TProgram extends string = typeof SOLFX_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    protocol: TAccountMetas[1];
  };
  data: SetGuardianInstructionData;
};

export function parseSetGuardianInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetGuardianInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), protocol: getNextAccount() },
    data: getSetGuardianInstructionDataDecoder().decode(instruction.data),
  };
}
