/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getEnumDecoder,
  getEnumEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Take-profit or stop-loss.
 *
 * The two differ only in which side of the trigger price fires them, but they are named
 * separately because the *risk* they carry is not the same: a stop-loss that fails to fire
 * is a position that keeps losing, while a take-profit that fails to fire is a trader who is
 * merely annoyed. Naming them apart means monitoring can alert on one and not the other.
 */
export enum TriggerKind {
  TakeProfit,
  StopLoss,
}

export type TriggerKindArgs = TriggerKind;

export function getTriggerKindEncoder(): FixedSizeEncoder<TriggerKindArgs> {
  return getEnumEncoder(TriggerKind);
}

export function getTriggerKindDecoder(): FixedSizeDecoder<TriggerKind> {
  return getEnumDecoder(TriggerKind);
}

export function getTriggerKindCodec(): FixedSizeCodec<
  TriggerKindArgs,
  TriggerKind
> {
  return combineCodec(getTriggerKindEncoder(), getTriggerKindDecoder());
}
