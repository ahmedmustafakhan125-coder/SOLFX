/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getTriggerKindDecoder,
  getTriggerKindEncoder,
  type TriggerKind,
  type TriggerKindArgs,
} from "../types";

export const TRIGGER_ORDER_DISCRIMINATOR = new Uint8Array([
  236, 61, 42, 190, 152, 12, 106, 116,
]);

export function getTriggerOrderDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TRIGGER_ORDER_DISCRIMINATOR,
  );
}

export type TriggerOrder = {
  discriminator: ReadonlyUint8Array;
  position: Address;
  userAccount: Address;
  /** The wallet that placed it — also the only one that can cancel it. */
  authority: Address;
  marketIndex: number;
  /** Distinguishes several orders on one position: a TP and an SL are the usual pair. */
  orderId: number;
  kind: TriggerKind;
  /** At `PRICE_PRECISION`. Compared against the oracle price, not the fill price. */
  triggerPrice: bigint;
  /** Base units to close. May be less than the position, for scaling out. */
  sizeBase: bigint;
  createdAt: bigint;
  bump: number;
  reserved: ReadonlyUint8Array;
};

export type TriggerOrderArgs = {
  position: Address;
  userAccount: Address;
  /** The wallet that placed it — also the only one that can cancel it. */
  authority: Address;
  marketIndex: number;
  /** Distinguishes several orders on one position: a TP and an SL are the usual pair. */
  orderId: number;
  kind: TriggerKindArgs;
  /** At `PRICE_PRECISION`. Compared against the oracle price, not the fill price. */
  triggerPrice: number | bigint;
  /** Base units to close. May be less than the position, for scaling out. */
  sizeBase: number | bigint;
  createdAt: number | bigint;
  bump: number;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link TriggerOrderArgs} account data. */
export function getTriggerOrderEncoder(): FixedSizeEncoder<TriggerOrderArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["position", getAddressEncoder()],
      ["userAccount", getAddressEncoder()],
      ["authority", getAddressEncoder()],
      ["marketIndex", getU16Encoder()],
      ["orderId", getU8Encoder()],
      ["kind", getTriggerKindEncoder()],
      ["triggerPrice", getI64Encoder()],
      ["sizeBase", getU64Encoder()],
      ["createdAt", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: TRIGGER_ORDER_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link TriggerOrder} account data. */
export function getTriggerOrderDecoder(): FixedSizeDecoder<TriggerOrder> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["position", getAddressDecoder()],
    ["userAccount", getAddressDecoder()],
    ["authority", getAddressDecoder()],
    ["marketIndex", getU16Decoder()],
    ["orderId", getU8Decoder()],
    ["kind", getTriggerKindDecoder()],
    ["triggerPrice", getI64Decoder()],
    ["sizeBase", getU64Decoder()],
    ["createdAt", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

/** Gets the codec for {@link TriggerOrder} account data. */
export function getTriggerOrderCodec(): FixedSizeCodec<
  TriggerOrderArgs,
  TriggerOrder
> {
  return combineCodec(getTriggerOrderEncoder(), getTriggerOrderDecoder());
}

export function decodeTriggerOrder<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<TriggerOrder, TAddress>;
export function decodeTriggerOrder<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<TriggerOrder, TAddress>;
export function decodeTriggerOrder<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<TriggerOrder, TAddress> | MaybeAccount<TriggerOrder, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTriggerOrderDecoder(),
  );
}

export async function fetchTriggerOrder<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<TriggerOrder, TAddress>> {
  const maybeAccount = await fetchMaybeTriggerOrder(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTriggerOrder<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<TriggerOrder, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTriggerOrder(maybeAccount);
}

export async function fetchAllTriggerOrder(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<TriggerOrder>[]> {
  const maybeAccounts = await fetchAllMaybeTriggerOrder(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTriggerOrder(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<TriggerOrder>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeTriggerOrder(maybeAccount));
}

export function getTriggerOrderSize(): number {
  return 165;
}
