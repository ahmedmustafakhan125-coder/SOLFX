/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LP_POOL_DISCRIMINATOR = new Uint8Array([
  185, 127, 131, 141, 197, 198, 170, 147,
]);

export function getLpPoolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(LP_POOL_DISCRIMINATOR);
}

export type LpPool = {
  discriminator: ReadonlyUint8Array;
  /** `slpUSD`. Mint authority is this PDA. */
  lpMint: Address;
  /** USDC token account owned by this PDA. */
  lpVault: Address;
  /**
   * Assets under management, at `QUOTE_PRECISION`. Invariant I2 requires this to equal
   * the vault's token balance.
   */
  aum: bigint;
  /** `slpUSD` in circulation. Invariant I8: supply > 0 ⟺ aum > 0. */
  lpTokenSupply: bigint;
  /**
   * Seconds between `request_remove_liquidity` and `remove_liquidity`.
   *
   * Anti-JIT (threat T6). Without it an LP deposits ahead of a known trader loss and
   * withdraws ahead of a known gain, harvesting the pool's edge without carrying its
   * risk.
   */
  withdrawalCooldownSeconds: number;
  /** Charged on exit, in bps. */
  exitFeeBps: number;
  /** High-water mark for the LP performance fee (§ 8.1, stream 4). */
  highWaterMarkPerShare: bigint;
  /** Performance fee in bps, charged on the gain above the high-water mark. § 8.1 sets 10%. */
  performanceFeeBps: number;
  /**
   * Shares with a withdrawal request outstanding.
   *
   * Tracked so the pool can report how much of its supply is queued to leave — a figure
   * the frontend needs and a run is visible in.
   */
  pendingWithdrawalShares: bigint;
  totalPerformanceFees: bigint;
  totalExitFees: bigint;
  /**
   * Cumulative USDC deposited by providers, and cumulative USDC paid back out.
   *
   * Withdrawals are one of the few ways USDC crosses the protocol boundary, so invariant
   * I7 — *every USDC the program holds is accounted for* — needs the outflow as a term.
   * The pair also gives the LP dashboard lifetime inflow and outflow without walking the
   * event stream.
   */
  totalDeposited: bigint;
  totalWithdrawn: bigint;
  bump: number;
  lpVaultBump: number;
  lpMintBump: number;
  reserved: ReadonlyUint8Array;
};

export type LpPoolArgs = {
  /** `slpUSD`. Mint authority is this PDA. */
  lpMint: Address;
  /** USDC token account owned by this PDA. */
  lpVault: Address;
  /**
   * Assets under management, at `QUOTE_PRECISION`. Invariant I2 requires this to equal
   * the vault's token balance.
   */
  aum: number | bigint;
  /** `slpUSD` in circulation. Invariant I8: supply > 0 ⟺ aum > 0. */
  lpTokenSupply: number | bigint;
  /**
   * Seconds between `request_remove_liquidity` and `remove_liquidity`.
   *
   * Anti-JIT (threat T6). Without it an LP deposits ahead of a known trader loss and
   * withdraws ahead of a known gain, harvesting the pool's edge without carrying its
   * risk.
   */
  withdrawalCooldownSeconds: number;
  /** Charged on exit, in bps. */
  exitFeeBps: number;
  /** High-water mark for the LP performance fee (§ 8.1, stream 4). */
  highWaterMarkPerShare: number | bigint;
  /** Performance fee in bps, charged on the gain above the high-water mark. § 8.1 sets 10%. */
  performanceFeeBps: number;
  /**
   * Shares with a withdrawal request outstanding.
   *
   * Tracked so the pool can report how much of its supply is queued to leave — a figure
   * the frontend needs and a run is visible in.
   */
  pendingWithdrawalShares: number | bigint;
  totalPerformanceFees: number | bigint;
  totalExitFees: number | bigint;
  /**
   * Cumulative USDC deposited by providers, and cumulative USDC paid back out.
   *
   * Withdrawals are one of the few ways USDC crosses the protocol boundary, so invariant
   * I7 — *every USDC the program holds is accounted for* — needs the outflow as a term.
   * The pair also gives the LP dashboard lifetime inflow and outflow without walking the
   * event stream.
   */
  totalDeposited: number | bigint;
  totalWithdrawn: number | bigint;
  bump: number;
  lpVaultBump: number;
  lpMintBump: number;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link LpPoolArgs} account data. */
export function getLpPoolEncoder(): FixedSizeEncoder<LpPoolArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["lpMint", getAddressEncoder()],
      ["lpVault", getAddressEncoder()],
      ["aum", getU64Encoder()],
      ["lpTokenSupply", getU64Encoder()],
      ["withdrawalCooldownSeconds", getU32Encoder()],
      ["exitFeeBps", getU16Encoder()],
      ["highWaterMarkPerShare", getU64Encoder()],
      ["performanceFeeBps", getU16Encoder()],
      ["pendingWithdrawalShares", getU64Encoder()],
      ["totalPerformanceFees", getU64Encoder()],
      ["totalExitFees", getU64Encoder()],
      ["totalDeposited", getU64Encoder()],
      ["totalWithdrawn", getU64Encoder()],
      ["bump", getU8Encoder()],
      ["lpVaultBump", getU8Encoder()],
      ["lpMintBump", getU8Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 80)],
    ]),
    (value) => ({ ...value, discriminator: LP_POOL_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link LpPool} account data. */
export function getLpPoolDecoder(): FixedSizeDecoder<LpPool> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["lpMint", getAddressDecoder()],
    ["lpVault", getAddressDecoder()],
    ["aum", getU64Decoder()],
    ["lpTokenSupply", getU64Decoder()],
    ["withdrawalCooldownSeconds", getU32Decoder()],
    ["exitFeeBps", getU16Decoder()],
    ["highWaterMarkPerShare", getU64Decoder()],
    ["performanceFeeBps", getU16Decoder()],
    ["pendingWithdrawalShares", getU64Decoder()],
    ["totalPerformanceFees", getU64Decoder()],
    ["totalExitFees", getU64Decoder()],
    ["totalDeposited", getU64Decoder()],
    ["totalWithdrawn", getU64Decoder()],
    ["bump", getU8Decoder()],
    ["lpVaultBump", getU8Decoder()],
    ["lpMintBump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 80)],
  ]);
}

/** Gets the codec for {@link LpPool} account data. */
export function getLpPoolCodec(): FixedSizeCodec<LpPoolArgs, LpPool> {
  return combineCodec(getLpPoolEncoder(), getLpPoolDecoder());
}

export function decodeLpPool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<LpPool, TAddress>;
export function decodeLpPool<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<LpPool, TAddress>;
export function decodeLpPool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<LpPool, TAddress> | MaybeAccount<LpPool, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getLpPoolDecoder(),
  );
}

export async function fetchLpPool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<LpPool, TAddress>> {
  const maybeAccount = await fetchMaybeLpPool(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeLpPool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<LpPool, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeLpPool(maybeAccount);
}

export async function fetchAllLpPool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<LpPool>[]> {
  const maybeAccounts = await fetchAllMaybeLpPool(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeLpPool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<LpPool>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeLpPool(maybeAccount));
}

export function getLpPoolSize(): number {
  return 227;
}
