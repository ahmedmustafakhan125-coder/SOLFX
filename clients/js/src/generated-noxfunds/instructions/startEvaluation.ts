/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findConfigPda,
  findStakeVaultPda,
  findTraderProfilePda,
} from "../pdas";
import { NOXFUNDS_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const START_EVALUATION_DISCRIMINATOR = new Uint8Array([
  155, 126, 223, 83, 56, 34, 155, 210,
]);

export function getStartEvaluationDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    START_EVALUATION_DISCRIMINATOR,
  );
}

export type StartEvaluationInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountTrader extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountTraderProfile extends string | AccountMeta<string> = string,
  TAccountEvaluation extends string | AccountMeta<string> = string,
  TAccountUsdcMint extends string | AccountMeta<string> = string,
  TAccountTraderToken extends string | AccountMeta<string> = string,
  TAccountStakeVault extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTrader extends string
        ? WritableSignerAccount<TAccountTrader> &
            AccountSignerMeta<TAccountTrader>
        : TAccountTrader,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountTraderProfile extends string
        ? ReadonlyAccount<TAccountTraderProfile>
        : TAccountTraderProfile,
      TAccountEvaluation extends string
        ? WritableAccount<TAccountEvaluation>
        : TAccountEvaluation,
      TAccountUsdcMint extends string
        ? ReadonlyAccount<TAccountUsdcMint>
        : TAccountUsdcMint,
      TAccountTraderToken extends string
        ? WritableAccount<TAccountTraderToken>
        : TAccountTraderToken,
      TAccountStakeVault extends string
        ? WritableAccount<TAccountStakeVault>
        : TAccountStakeVault,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type StartEvaluationInstructionData = {
  discriminator: ReadonlyUint8Array;
  seq: number;
  accountSize: bigint;
};

export type StartEvaluationInstructionDataArgs = {
  seq: number;
  accountSize: number | bigint;
};

export function getStartEvaluationInstructionDataEncoder(): FixedSizeEncoder<StartEvaluationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["seq", getU8Encoder()],
      ["accountSize", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: START_EVALUATION_DISCRIMINATOR }),
  );
}

export function getStartEvaluationInstructionDataDecoder(): FixedSizeDecoder<StartEvaluationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["seq", getU8Decoder()],
    ["accountSize", getU64Decoder()],
  ]);
}

export function getStartEvaluationInstructionDataCodec(): FixedSizeCodec<
  StartEvaluationInstructionDataArgs,
  StartEvaluationInstructionData
> {
  return combineCodec(
    getStartEvaluationInstructionDataEncoder(),
    getStartEvaluationInstructionDataDecoder(),
  );
}

export type StartEvaluationAsyncInput<
  TAccountTrader extends string = string,
  TAccountConfig extends string = string,
  TAccountTraderProfile extends string = string,
  TAccountEvaluation extends string = string,
  TAccountUsdcMint extends string = string,
  TAccountTraderToken extends string = string,
  TAccountStakeVault extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  trader: TransactionSigner<TAccountTrader>;
  config?: Address<TAccountConfig>;
  /**
   * Required: passing is recorded against a real profile, and an evaluation run by a wallet
   * with no profile would earn a record nobody could find.
   */
  traderProfile?: Address<TAccountTraderProfile>;
  evaluation: Address<TAccountEvaluation>;
  usdcMint: Address<TAccountUsdcMint>;
  traderToken: Address<TAccountTraderToken>;
  /**
   * The stake escrow, owned by the evaluation PDA. Nobody else can move it: refunded to the
   * trader on a pass, forfeited to the treasury on a failure, and there is no third door.
   */
  stakeVault?: Address<TAccountStakeVault>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  seq: StartEvaluationInstructionDataArgs["seq"];
  accountSize: StartEvaluationInstructionDataArgs["accountSize"];
};

export async function getStartEvaluationInstructionAsync<
  TAccountTrader extends string,
  TAccountConfig extends string,
  TAccountTraderProfile extends string,
  TAccountEvaluation extends string,
  TAccountUsdcMint extends string,
  TAccountTraderToken extends string,
  TAccountStakeVault extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NOXFUNDS_PROGRAM_ADDRESS,
>(
  input: StartEvaluationAsyncInput<
    TAccountTrader,
    TAccountConfig,
    TAccountTraderProfile,
    TAccountEvaluation,
    TAccountUsdcMint,
    TAccountTraderToken,
    TAccountStakeVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  StartEvaluationInstruction<
    TProgramAddress,
    TAccountTrader,
    TAccountConfig,
    TAccountTraderProfile,
    TAccountEvaluation,
    TAccountUsdcMint,
    TAccountTraderToken,
    TAccountStakeVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? NOXFUNDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trader: { value: input.trader ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    traderProfile: { value: input.traderProfile ?? null, isWritable: false },
    evaluation: { value: input.evaluation ?? null, isWritable: true },
    usdcMint: { value: input.usdcMint ?? null, isWritable: false },
    traderToken: { value: input.traderToken ?? null, isWritable: true },
    stakeVault: { value: input.stakeVault ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.traderProfile.value) {
    accounts.traderProfile.value = await findTraderProfilePda({
      trader: expectAddress(accounts.trader.value),
    });
  }
  if (!accounts.stakeVault.value) {
    accounts.stakeVault.value = await findStakeVaultPda({
      evaluation: expectAddress(accounts.evaluation.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.trader),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.traderProfile),
      getAccountMeta(accounts.evaluation),
      getAccountMeta(accounts.usdcMint),
      getAccountMeta(accounts.traderToken),
      getAccountMeta(accounts.stakeVault),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getStartEvaluationInstructionDataEncoder().encode(
      args as StartEvaluationInstructionDataArgs,
    ),
    programAddress,
  } as StartEvaluationInstruction<
    TProgramAddress,
    TAccountTrader,
    TAccountConfig,
    TAccountTraderProfile,
    TAccountEvaluation,
    TAccountUsdcMint,
    TAccountTraderToken,
    TAccountStakeVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type StartEvaluationInput<
  TAccountTrader extends string = string,
  TAccountConfig extends string = string,
  TAccountTraderProfile extends string = string,
  TAccountEvaluation extends string = string,
  TAccountUsdcMint extends string = string,
  TAccountTraderToken extends string = string,
  TAccountStakeVault extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  trader: TransactionSigner<TAccountTrader>;
  config: Address<TAccountConfig>;
  /**
   * Required: passing is recorded against a real profile, and an evaluation run by a wallet
   * with no profile would earn a record nobody could find.
   */
  traderProfile: Address<TAccountTraderProfile>;
  evaluation: Address<TAccountEvaluation>;
  usdcMint: Address<TAccountUsdcMint>;
  traderToken: Address<TAccountTraderToken>;
  /**
   * The stake escrow, owned by the evaluation PDA. Nobody else can move it: refunded to the
   * trader on a pass, forfeited to the treasury on a failure, and there is no third door.
   */
  stakeVault: Address<TAccountStakeVault>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  seq: StartEvaluationInstructionDataArgs["seq"];
  accountSize: StartEvaluationInstructionDataArgs["accountSize"];
};

export function getStartEvaluationInstruction<
  TAccountTrader extends string,
  TAccountConfig extends string,
  TAccountTraderProfile extends string,
  TAccountEvaluation extends string,
  TAccountUsdcMint extends string,
  TAccountTraderToken extends string,
  TAccountStakeVault extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NOXFUNDS_PROGRAM_ADDRESS,
>(
  input: StartEvaluationInput<
    TAccountTrader,
    TAccountConfig,
    TAccountTraderProfile,
    TAccountEvaluation,
    TAccountUsdcMint,
    TAccountTraderToken,
    TAccountStakeVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): StartEvaluationInstruction<
  TProgramAddress,
  TAccountTrader,
  TAccountConfig,
  TAccountTraderProfile,
  TAccountEvaluation,
  TAccountUsdcMint,
  TAccountTraderToken,
  TAccountStakeVault,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? NOXFUNDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trader: { value: input.trader ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    traderProfile: { value: input.traderProfile ?? null, isWritable: false },
    evaluation: { value: input.evaluation ?? null, isWritable: true },
    usdcMint: { value: input.usdcMint ?? null, isWritable: false },
    traderToken: { value: input.traderToken ?? null, isWritable: true },
    stakeVault: { value: input.stakeVault ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.trader),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.traderProfile),
      getAccountMeta(accounts.evaluation),
      getAccountMeta(accounts.usdcMint),
      getAccountMeta(accounts.traderToken),
      getAccountMeta(accounts.stakeVault),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getStartEvaluationInstructionDataEncoder().encode(
      args as StartEvaluationInstructionDataArgs,
    ),
    programAddress,
  } as StartEvaluationInstruction<
    TProgramAddress,
    TAccountTrader,
    TAccountConfig,
    TAccountTraderProfile,
    TAccountEvaluation,
    TAccountUsdcMint,
    TAccountTraderToken,
    TAccountStakeVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedStartEvaluationInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    trader: TAccountMetas[0];
    config: TAccountMetas[1];
    /**
     * Required: passing is recorded against a real profile, and an evaluation run by a wallet
     * with no profile would earn a record nobody could find.
     */
    traderProfile: TAccountMetas[2];
    evaluation: TAccountMetas[3];
    usdcMint: TAccountMetas[4];
    traderToken: TAccountMetas[5];
    /**
     * The stake escrow, owned by the evaluation PDA. Nobody else can move it: refunded to the
     * trader on a pass, forfeited to the treasury on a failure, and there is no third door.
     */
    stakeVault: TAccountMetas[6];
    tokenProgram: TAccountMetas[7];
    systemProgram: TAccountMetas[8];
  };
  data: StartEvaluationInstructionData;
};

export function parseStartEvaluationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedStartEvaluationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      trader: getNextAccount(),
      config: getNextAccount(),
      traderProfile: getNextAccount(),
      evaluation: getNextAccount(),
      usdcMint: getNextAccount(),
      traderToken: getNextAccount(),
      stakeVault: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getStartEvaluationInstructionDataDecoder().decode(instruction.data),
  };
}
