/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  findConfigPda,
  findMandateSignerPda,
  findMandateVaultPda,
} from "../pdas";
import { NOXFUNDS_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLAIM_SETTLEMENT_DISCRIMINATOR = new Uint8Array([
  85, 208, 73, 229, 143, 98, 83, 212,
]);

export function getClaimSettlementDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_SETTLEMENT_DISCRIMINATOR,
  );
}

export type ClaimSettlementInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountSettler extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountMandate extends string | AccountMeta<string> = string,
  TAccountMandateSigner extends string | AccountMeta<string> = string,
  TAccountProtocol extends string | AccountMeta<string> = string,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TAccountUsdcMint extends string | AccountMeta<string> = string,
  TAccountCollateralVault extends string | AccountMeta<string> = string,
  TAccountMandateVault extends string | AccountMeta<string> = string,
  TAccountInvestorToken extends string | AccountMeta<string> = string,
  TAccountTraderToken extends string | AccountMeta<string> = string,
  TAccountTreasuryToken extends string | AccountMeta<string> = string,
  TAccountTraderProfile extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSolfxCoreProgram extends string | AccountMeta<string> =
    "2EQzy2Mzixi54tJkMbWWqJFoayUoGBNwZCEJCy44ZVKi",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSettler extends string
        ? ReadonlySignerAccount<TAccountSettler> &
            AccountSignerMeta<TAccountSettler>
        : TAccountSettler,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountMandate extends string
        ? WritableAccount<TAccountMandate>
        : TAccountMandate,
      TAccountMandateSigner extends string
        ? WritableAccount<TAccountMandateSigner>
        : TAccountMandateSigner,
      TAccountProtocol extends string
        ? WritableAccount<TAccountProtocol>
        : TAccountProtocol,
      TAccountUserAccount extends string
        ? WritableAccount<TAccountUserAccount>
        : TAccountUserAccount,
      TAccountUsdcMint extends string
        ? WritableAccount<TAccountUsdcMint>
        : TAccountUsdcMint,
      TAccountCollateralVault extends string
        ? WritableAccount<TAccountCollateralVault>
        : TAccountCollateralVault,
      TAccountMandateVault extends string
        ? WritableAccount<TAccountMandateVault>
        : TAccountMandateVault,
      TAccountInvestorToken extends string
        ? WritableAccount<TAccountInvestorToken>
        : TAccountInvestorToken,
      TAccountTraderToken extends string
        ? WritableAccount<TAccountTraderToken>
        : TAccountTraderToken,
      TAccountTreasuryToken extends string
        ? WritableAccount<TAccountTreasuryToken>
        : TAccountTreasuryToken,
      TAccountTraderProfile extends string
        ? WritableAccount<TAccountTraderProfile>
        : TAccountTraderProfile,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSolfxCoreProgram extends string
        ? ReadonlyAccount<TAccountSolfxCoreProgram>
        : TAccountSolfxCoreProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimSettlementInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClaimSettlementInstructionDataArgs = {};

export function getClaimSettlementInstructionDataEncoder(): FixedSizeEncoder<ClaimSettlementInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLAIM_SETTLEMENT_DISCRIMINATOR }),
  );
}

export function getClaimSettlementInstructionDataDecoder(): FixedSizeDecoder<ClaimSettlementInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClaimSettlementInstructionDataCodec(): FixedSizeCodec<
  ClaimSettlementInstructionDataArgs,
  ClaimSettlementInstructionData
> {
  return combineCodec(
    getClaimSettlementInstructionDataEncoder(),
    getClaimSettlementInstructionDataDecoder(),
  );
}

export type ClaimSettlementAsyncInput<
  TAccountSettler extends string = string,
  TAccountConfig extends string = string,
  TAccountMandate extends string = string,
  TAccountMandateSigner extends string = string,
  TAccountProtocol extends string = string,
  TAccountUserAccount extends string = string,
  TAccountUsdcMint extends string = string,
  TAccountCollateralVault extends string = string,
  TAccountMandateVault extends string = string,
  TAccountInvestorToken extends string = string,
  TAccountTraderToken extends string = string,
  TAccountTreasuryToken extends string = string,
  TAccountTraderProfile extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSolfxCoreProgram extends string = string,
> = {
  /** **Anyone.** Pays the transaction fee and nothing else. */
  settler: TransactionSigner<TAccountSettler>;
  config?: Address<TAccountConfig>;
  mandate: Address<TAccountMandate>;
  mandateSigner?: Address<TAccountMandateSigner>;
  protocol: Address<TAccountProtocol>;
  /** Deserialized: settlement withdraws exactly the free collateral recorded here. */
  userAccount: Address<TAccountUserAccount>;
  /**
   * `mut` because `solfx-core`'s `MoveCollateral` marks it so — a CPI cannot ask for a
   * privilege the outer instruction did not grant.
   */
  usdcMint: Address<TAccountUsdcMint>;
  collateralVault: Address<TAccountCollateralVault>;
  /**
   * The mandate's own USDC account. Withdrawals land here first, then leave in three parts.
   *
   * Bound to `["vault", mandate]`. It used to be *any* token account the signer owned, and
   * the settler — who is anyone — chose which: they could pass a fresh empty account, so the
   * split was computed over a balance that excluded any principal still sitting in the real
   * vault.
   */
  mandateVault?: Address<TAccountMandateVault>;
  /**
   * Constrained to the investor recorded at funding, not to whoever calls — a permissionless
   * instruction must not let the caller choose where the principal goes.
   */
  investorToken: Address<TAccountInvestorToken>;
  traderToken: Address<TAccountTraderToken>;
  treasuryToken: Address<TAccountTreasuryToken>;
  /**
   * The trader's record. Settlement is where a mandate's outcome — not just its individual
   * trades — lands on it: the active count comes down, and a mandate that finished above
   * principal counts toward Platinum.
   */
  traderProfile: Address<TAccountTraderProfile>;
  tokenProgram?: Address<TAccountTokenProgram>;
  solfxCoreProgram?: Address<TAccountSolfxCoreProgram>;
};

export async function getClaimSettlementInstructionAsync<
  TAccountSettler extends string,
  TAccountConfig extends string,
  TAccountMandate extends string,
  TAccountMandateSigner extends string,
  TAccountProtocol extends string,
  TAccountUserAccount extends string,
  TAccountUsdcMint extends string,
  TAccountCollateralVault extends string,
  TAccountMandateVault extends string,
  TAccountInvestorToken extends string,
  TAccountTraderToken extends string,
  TAccountTreasuryToken extends string,
  TAccountTraderProfile extends string,
  TAccountTokenProgram extends string,
  TAccountSolfxCoreProgram extends string,
  TProgramAddress extends Address = typeof NOXFUNDS_PROGRAM_ADDRESS,
>(
  input: ClaimSettlementAsyncInput<
    TAccountSettler,
    TAccountConfig,
    TAccountMandate,
    TAccountMandateSigner,
    TAccountProtocol,
    TAccountUserAccount,
    TAccountUsdcMint,
    TAccountCollateralVault,
    TAccountMandateVault,
    TAccountInvestorToken,
    TAccountTraderToken,
    TAccountTreasuryToken,
    TAccountTraderProfile,
    TAccountTokenProgram,
    TAccountSolfxCoreProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClaimSettlementInstruction<
    TProgramAddress,
    TAccountSettler,
    TAccountConfig,
    TAccountMandate,
    TAccountMandateSigner,
    TAccountProtocol,
    TAccountUserAccount,
    TAccountUsdcMint,
    TAccountCollateralVault,
    TAccountMandateVault,
    TAccountInvestorToken,
    TAccountTraderToken,
    TAccountTreasuryToken,
    TAccountTraderProfile,
    TAccountTokenProgram,
    TAccountSolfxCoreProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? NOXFUNDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    settler: { value: input.settler ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    mandate: { value: input.mandate ?? null, isWritable: true },
    mandateSigner: { value: input.mandateSigner ?? null, isWritable: true },
    protocol: { value: input.protocol ?? null, isWritable: true },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    usdcMint: { value: input.usdcMint ?? null, isWritable: true },
    collateralVault: { value: input.collateralVault ?? null, isWritable: true },
    mandateVault: { value: input.mandateVault ?? null, isWritable: true },
    investorToken: { value: input.investorToken ?? null, isWritable: true },
    traderToken: { value: input.traderToken ?? null, isWritable: true },
    treasuryToken: { value: input.treasuryToken ?? null, isWritable: true },
    traderProfile: { value: input.traderProfile ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    solfxCoreProgram: {
      value: input.solfxCoreProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.mandateSigner.value) {
    accounts.mandateSigner.value = await findMandateSignerPda({
      mandate: expectAddress(accounts.mandate.value),
    });
  }
  if (!accounts.mandateVault.value) {
    accounts.mandateVault.value = await findMandateVaultPda({
      mandate: expectAddress(accounts.mandate.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.solfxCoreProgram.value) {
    accounts.solfxCoreProgram.value =
      "2EQzy2Mzixi54tJkMbWWqJFoayUoGBNwZCEJCy44ZVKi" as Address<"2EQzy2Mzixi54tJkMbWWqJFoayUoGBNwZCEJCy44ZVKi">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.settler),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.mandate),
      getAccountMeta(accounts.mandateSigner),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.usdcMint),
      getAccountMeta(accounts.collateralVault),
      getAccountMeta(accounts.mandateVault),
      getAccountMeta(accounts.investorToken),
      getAccountMeta(accounts.traderToken),
      getAccountMeta(accounts.treasuryToken),
      getAccountMeta(accounts.traderProfile),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.solfxCoreProgram),
    ],
    data: getClaimSettlementInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimSettlementInstruction<
    TProgramAddress,
    TAccountSettler,
    TAccountConfig,
    TAccountMandate,
    TAccountMandateSigner,
    TAccountProtocol,
    TAccountUserAccount,
    TAccountUsdcMint,
    TAccountCollateralVault,
    TAccountMandateVault,
    TAccountInvestorToken,
    TAccountTraderToken,
    TAccountTreasuryToken,
    TAccountTraderProfile,
    TAccountTokenProgram,
    TAccountSolfxCoreProgram
  >);
}

export type ClaimSettlementInput<
  TAccountSettler extends string = string,
  TAccountConfig extends string = string,
  TAccountMandate extends string = string,
  TAccountMandateSigner extends string = string,
  TAccountProtocol extends string = string,
  TAccountUserAccount extends string = string,
  TAccountUsdcMint extends string = string,
  TAccountCollateralVault extends string = string,
  TAccountMandateVault extends string = string,
  TAccountInvestorToken extends string = string,
  TAccountTraderToken extends string = string,
  TAccountTreasuryToken extends string = string,
  TAccountTraderProfile extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSolfxCoreProgram extends string = string,
> = {
  /** **Anyone.** Pays the transaction fee and nothing else. */
  settler: TransactionSigner<TAccountSettler>;
  config: Address<TAccountConfig>;
  mandate: Address<TAccountMandate>;
  mandateSigner: Address<TAccountMandateSigner>;
  protocol: Address<TAccountProtocol>;
  /** Deserialized: settlement withdraws exactly the free collateral recorded here. */
  userAccount: Address<TAccountUserAccount>;
  /**
   * `mut` because `solfx-core`'s `MoveCollateral` marks it so — a CPI cannot ask for a
   * privilege the outer instruction did not grant.
   */
  usdcMint: Address<TAccountUsdcMint>;
  collateralVault: Address<TAccountCollateralVault>;
  /**
   * The mandate's own USDC account. Withdrawals land here first, then leave in three parts.
   *
   * Bound to `["vault", mandate]`. It used to be *any* token account the signer owned, and
   * the settler — who is anyone — chose which: they could pass a fresh empty account, so the
   * split was computed over a balance that excluded any principal still sitting in the real
   * vault.
   */
  mandateVault: Address<TAccountMandateVault>;
  /**
   * Constrained to the investor recorded at funding, not to whoever calls — a permissionless
   * instruction must not let the caller choose where the principal goes.
   */
  investorToken: Address<TAccountInvestorToken>;
  traderToken: Address<TAccountTraderToken>;
  treasuryToken: Address<TAccountTreasuryToken>;
  /**
   * The trader's record. Settlement is where a mandate's outcome — not just its individual
   * trades — lands on it: the active count comes down, and a mandate that finished above
   * principal counts toward Platinum.
   */
  traderProfile: Address<TAccountTraderProfile>;
  tokenProgram?: Address<TAccountTokenProgram>;
  solfxCoreProgram?: Address<TAccountSolfxCoreProgram>;
};

export function getClaimSettlementInstruction<
  TAccountSettler extends string,
  TAccountConfig extends string,
  TAccountMandate extends string,
  TAccountMandateSigner extends string,
  TAccountProtocol extends string,
  TAccountUserAccount extends string,
  TAccountUsdcMint extends string,
  TAccountCollateralVault extends string,
  TAccountMandateVault extends string,
  TAccountInvestorToken extends string,
  TAccountTraderToken extends string,
  TAccountTreasuryToken extends string,
  TAccountTraderProfile extends string,
  TAccountTokenProgram extends string,
  TAccountSolfxCoreProgram extends string,
  TProgramAddress extends Address = typeof NOXFUNDS_PROGRAM_ADDRESS,
>(
  input: ClaimSettlementInput<
    TAccountSettler,
    TAccountConfig,
    TAccountMandate,
    TAccountMandateSigner,
    TAccountProtocol,
    TAccountUserAccount,
    TAccountUsdcMint,
    TAccountCollateralVault,
    TAccountMandateVault,
    TAccountInvestorToken,
    TAccountTraderToken,
    TAccountTreasuryToken,
    TAccountTraderProfile,
    TAccountTokenProgram,
    TAccountSolfxCoreProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ClaimSettlementInstruction<
  TProgramAddress,
  TAccountSettler,
  TAccountConfig,
  TAccountMandate,
  TAccountMandateSigner,
  TAccountProtocol,
  TAccountUserAccount,
  TAccountUsdcMint,
  TAccountCollateralVault,
  TAccountMandateVault,
  TAccountInvestorToken,
  TAccountTraderToken,
  TAccountTreasuryToken,
  TAccountTraderProfile,
  TAccountTokenProgram,
  TAccountSolfxCoreProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? NOXFUNDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    settler: { value: input.settler ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    mandate: { value: input.mandate ?? null, isWritable: true },
    mandateSigner: { value: input.mandateSigner ?? null, isWritable: true },
    protocol: { value: input.protocol ?? null, isWritable: true },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    usdcMint: { value: input.usdcMint ?? null, isWritable: true },
    collateralVault: { value: input.collateralVault ?? null, isWritable: true },
    mandateVault: { value: input.mandateVault ?? null, isWritable: true },
    investorToken: { value: input.investorToken ?? null, isWritable: true },
    traderToken: { value: input.traderToken ?? null, isWritable: true },
    treasuryToken: { value: input.treasuryToken ?? null, isWritable: true },
    traderProfile: { value: input.traderProfile ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    solfxCoreProgram: {
      value: input.solfxCoreProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.solfxCoreProgram.value) {
    accounts.solfxCoreProgram.value =
      "2EQzy2Mzixi54tJkMbWWqJFoayUoGBNwZCEJCy44ZVKi" as Address<"2EQzy2Mzixi54tJkMbWWqJFoayUoGBNwZCEJCy44ZVKi">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.settler),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.mandate),
      getAccountMeta(accounts.mandateSigner),
      getAccountMeta(accounts.protocol),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.usdcMint),
      getAccountMeta(accounts.collateralVault),
      getAccountMeta(accounts.mandateVault),
      getAccountMeta(accounts.investorToken),
      getAccountMeta(accounts.traderToken),
      getAccountMeta(accounts.treasuryToken),
      getAccountMeta(accounts.traderProfile),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.solfxCoreProgram),
    ],
    data: getClaimSettlementInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimSettlementInstruction<
    TProgramAddress,
    TAccountSettler,
    TAccountConfig,
    TAccountMandate,
    TAccountMandateSigner,
    TAccountProtocol,
    TAccountUserAccount,
    TAccountUsdcMint,
    TAccountCollateralVault,
    TAccountMandateVault,
    TAccountInvestorToken,
    TAccountTraderToken,
    TAccountTreasuryToken,
    TAccountTraderProfile,
    TAccountTokenProgram,
    TAccountSolfxCoreProgram
  >);
}

export type ParsedClaimSettlementInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** **Anyone.** Pays the transaction fee and nothing else. */
    settler: TAccountMetas[0];
    config: TAccountMetas[1];
    mandate: TAccountMetas[2];
    mandateSigner: TAccountMetas[3];
    protocol: TAccountMetas[4];
    /** Deserialized: settlement withdraws exactly the free collateral recorded here. */
    userAccount: TAccountMetas[5];
    /**
     * `mut` because `solfx-core`'s `MoveCollateral` marks it so — a CPI cannot ask for a
     * privilege the outer instruction did not grant.
     */
    usdcMint: TAccountMetas[6];
    collateralVault: TAccountMetas[7];
    /**
     * The mandate's own USDC account. Withdrawals land here first, then leave in three parts.
     *
     * Bound to `["vault", mandate]`. It used to be *any* token account the signer owned, and
     * the settler — who is anyone — chose which: they could pass a fresh empty account, so the
     * split was computed over a balance that excluded any principal still sitting in the real
     * vault.
     */
    mandateVault: TAccountMetas[8];
    /**
     * Constrained to the investor recorded at funding, not to whoever calls — a permissionless
     * instruction must not let the caller choose where the principal goes.
     */
    investorToken: TAccountMetas[9];
    traderToken: TAccountMetas[10];
    treasuryToken: TAccountMetas[11];
    /**
     * The trader's record. Settlement is where a mandate's outcome — not just its individual
     * trades — lands on it: the active count comes down, and a mandate that finished above
     * principal counts toward Platinum.
     */
    traderProfile: TAccountMetas[12];
    tokenProgram: TAccountMetas[13];
    solfxCoreProgram: TAccountMetas[14];
  };
  data: ClaimSettlementInstructionData;
};

export function parseClaimSettlementInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClaimSettlementInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 15) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      settler: getNextAccount(),
      config: getNextAccount(),
      mandate: getNextAccount(),
      mandateSigner: getNextAccount(),
      protocol: getNextAccount(),
      userAccount: getNextAccount(),
      usdcMint: getNextAccount(),
      collateralVault: getNextAccount(),
      mandateVault: getNextAccount(),
      investorToken: getNextAccount(),
      traderToken: getNextAccount(),
      treasuryToken: getNextAccount(),
      traderProfile: getNextAccount(),
      tokenProgram: getNextAccount(),
      solfxCoreProgram: getNextAccount(),
    },
    data: getClaimSettlementInstructionDataDecoder().decode(instruction.data),
  };
}
