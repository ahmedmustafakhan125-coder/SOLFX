/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { NOXFUNDS_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const RECOMPUTE_TIER_DISCRIMINATOR = new Uint8Array([
  88, 190, 225, 127, 27, 185, 123, 147,
]);

export function getRecomputeTierDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECOMPUTE_TIER_DISCRIMINATOR,
  );
}

export type RecomputeTierInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountProfile extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountProfile extends string
        ? WritableAccount<TAccountProfile>
        : TAccountProfile,
      ...TRemainingAccounts,
    ]
  >;

export type RecomputeTierInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RecomputeTierInstructionDataArgs = {};

export function getRecomputeTierInstructionDataEncoder(): FixedSizeEncoder<RecomputeTierInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RECOMPUTE_TIER_DISCRIMINATOR }),
  );
}

export function getRecomputeTierInstructionDataDecoder(): FixedSizeDecoder<RecomputeTierInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRecomputeTierInstructionDataCodec(): FixedSizeCodec<
  RecomputeTierInstructionDataArgs,
  RecomputeTierInstructionData
> {
  return combineCodec(
    getRecomputeTierInstructionDataEncoder(),
    getRecomputeTierInstructionDataDecoder(),
  );
}

export type RecomputeTierInput<
  TAccountCaller extends string = string,
  TAccountProfile extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  profile: Address<TAccountProfile>;
};

export function getRecomputeTierInstruction<
  TAccountCaller extends string,
  TAccountProfile extends string,
  TProgramAddress extends Address = typeof NOXFUNDS_PROGRAM_ADDRESS,
>(
  input: RecomputeTierInput<TAccountCaller, TAccountProfile>,
  config?: { programAddress?: TProgramAddress },
): RecomputeTierInstruction<TProgramAddress, TAccountCaller, TAccountProfile> {
  // Program address.
  const programAddress = config?.programAddress ?? NOXFUNDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    profile: { value: input.profile ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.profile),
    ],
    data: getRecomputeTierInstructionDataEncoder().encode({}),
    programAddress,
  } as RecomputeTierInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountProfile
  >);
}

export type ParsedRecomputeTierInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    caller: TAccountMetas[0];
    profile: TAccountMetas[1];
  };
  data: RecomputeTierInstructionData;
};

export function parseRecomputeTierInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRecomputeTierInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { caller: getNextAccount(), profile: getNextAccount() },
    data: getRecomputeTierInstructionDataDecoder().decode(instruction.data),
  };
}
