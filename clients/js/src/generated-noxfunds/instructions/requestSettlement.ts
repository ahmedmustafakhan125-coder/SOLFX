/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { NOXFUNDS_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const REQUEST_SETTLEMENT_DISCRIMINATOR = new Uint8Array([
  238, 127, 110, 105, 43, 95, 115, 41,
]);

export function getRequestSettlementDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REQUEST_SETTLEMENT_DISCRIMINATOR,
  );
}

export type RequestSettlementInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountInvestor extends string | AccountMeta<string> = string,
  TAccountMandate extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountInvestor extends string
        ? ReadonlySignerAccount<TAccountInvestor> &
            AccountSignerMeta<TAccountInvestor>
        : TAccountInvestor,
      TAccountMandate extends string
        ? WritableAccount<TAccountMandate>
        : TAccountMandate,
      ...TRemainingAccounts,
    ]
  >;

export type RequestSettlementInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RequestSettlementInstructionDataArgs = {};

export function getRequestSettlementInstructionDataEncoder(): FixedSizeEncoder<RequestSettlementInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REQUEST_SETTLEMENT_DISCRIMINATOR }),
  );
}

export function getRequestSettlementInstructionDataDecoder(): FixedSizeDecoder<RequestSettlementInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRequestSettlementInstructionDataCodec(): FixedSizeCodec<
  RequestSettlementInstructionDataArgs,
  RequestSettlementInstructionData
> {
  return combineCodec(
    getRequestSettlementInstructionDataEncoder(),
    getRequestSettlementInstructionDataDecoder(),
  );
}

export type RequestSettlementInput<
  TAccountInvestor extends string = string,
  TAccountMandate extends string = string,
> = {
  investor: TransactionSigner<TAccountInvestor>;
  mandate: Address<TAccountMandate>;
};

export function getRequestSettlementInstruction<
  TAccountInvestor extends string,
  TAccountMandate extends string,
  TProgramAddress extends Address = typeof NOXFUNDS_PROGRAM_ADDRESS,
>(
  input: RequestSettlementInput<TAccountInvestor, TAccountMandate>,
  config?: { programAddress?: TProgramAddress },
): RequestSettlementInstruction<
  TProgramAddress,
  TAccountInvestor,
  TAccountMandate
> {
  // Program address.
  const programAddress = config?.programAddress ?? NOXFUNDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    investor: { value: input.investor ?? null, isWritable: false },
    mandate: { value: input.mandate ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.investor),
      getAccountMeta(accounts.mandate),
    ],
    data: getRequestSettlementInstructionDataEncoder().encode({}),
    programAddress,
  } as RequestSettlementInstruction<
    TProgramAddress,
    TAccountInvestor,
    TAccountMandate
  >);
}

export type ParsedRequestSettlementInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    investor: TAccountMetas[0];
    mandate: TAccountMetas[1];
  };
  data: RequestSettlementInstructionData;
};

export function parseRequestSettlementInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRequestSettlementInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { investor: getNextAccount(), mandate: getNextAccount() },
    data: getRequestSettlementInstructionDataDecoder().decode(instruction.data),
  };
}
