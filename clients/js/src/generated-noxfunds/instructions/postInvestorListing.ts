/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findConfigPda, findListingPda } from "../pdas";
import { NOXFUNDS_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";
import {
  getInvestorListingTermsDecoder,
  getInvestorListingTermsEncoder,
  type InvestorListingTerms,
  type InvestorListingTermsArgs,
} from "../types";

export const POST_INVESTOR_LISTING_DISCRIMINATOR = new Uint8Array([
  202, 230, 166, 98, 148, 139, 66, 144,
]);

export function getPostInvestorListingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    POST_INVESTOR_LISTING_DISCRIMINATOR,
  );
}

export type PostInvestorListingInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountInvestor extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountListing extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountInvestor extends string
        ? WritableSignerAccount<TAccountInvestor> &
            AccountSignerMeta<TAccountInvestor>
        : TAccountInvestor,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountListing extends string
        ? WritableAccount<TAccountListing>
        : TAccountListing,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PostInvestorListingInstructionData = {
  discriminator: ReadonlyUint8Array;
  terms: InvestorListingTerms;
};

export type PostInvestorListingInstructionDataArgs = {
  terms: InvestorListingTermsArgs;
};

export function getPostInvestorListingInstructionDataEncoder(): Encoder<PostInvestorListingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["terms", getInvestorListingTermsEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: POST_INVESTOR_LISTING_DISCRIMINATOR,
    }),
  );
}

export function getPostInvestorListingInstructionDataDecoder(): Decoder<PostInvestorListingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["terms", getInvestorListingTermsDecoder()],
  ]);
}

export function getPostInvestorListingInstructionDataCodec(): Codec<
  PostInvestorListingInstructionDataArgs,
  PostInvestorListingInstructionData
> {
  return combineCodec(
    getPostInvestorListingInstructionDataEncoder(),
    getPostInvestorListingInstructionDataDecoder(),
  );
}

export type PostInvestorListingAsyncInput<
  TAccountInvestor extends string = string,
  TAccountConfig extends string = string,
  TAccountListing extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  investor: TransactionSigner<TAccountInvestor>;
  config?: Address<TAccountConfig>;
  listing?: Address<TAccountListing>;
  systemProgram?: Address<TAccountSystemProgram>;
  terms: PostInvestorListingInstructionDataArgs["terms"];
};

export async function getPostInvestorListingInstructionAsync<
  TAccountInvestor extends string,
  TAccountConfig extends string,
  TAccountListing extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NOXFUNDS_PROGRAM_ADDRESS,
>(
  input: PostInvestorListingAsyncInput<
    TAccountInvestor,
    TAccountConfig,
    TAccountListing,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PostInvestorListingInstruction<
    TProgramAddress,
    TAccountInvestor,
    TAccountConfig,
    TAccountListing,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? NOXFUNDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    investor: { value: input.investor ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    listing: { value: input.listing ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.listing.value) {
    accounts.listing.value = await findListingPda({
      investor: expectAddress(accounts.investor.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.investor),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.listing),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getPostInvestorListingInstructionDataEncoder().encode(
      args as PostInvestorListingInstructionDataArgs,
    ),
    programAddress,
  } as PostInvestorListingInstruction<
    TProgramAddress,
    TAccountInvestor,
    TAccountConfig,
    TAccountListing,
    TAccountSystemProgram
  >);
}

export type PostInvestorListingInput<
  TAccountInvestor extends string = string,
  TAccountConfig extends string = string,
  TAccountListing extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  investor: TransactionSigner<TAccountInvestor>;
  config: Address<TAccountConfig>;
  listing: Address<TAccountListing>;
  systemProgram?: Address<TAccountSystemProgram>;
  terms: PostInvestorListingInstructionDataArgs["terms"];
};

export function getPostInvestorListingInstruction<
  TAccountInvestor extends string,
  TAccountConfig extends string,
  TAccountListing extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NOXFUNDS_PROGRAM_ADDRESS,
>(
  input: PostInvestorListingInput<
    TAccountInvestor,
    TAccountConfig,
    TAccountListing,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PostInvestorListingInstruction<
  TProgramAddress,
  TAccountInvestor,
  TAccountConfig,
  TAccountListing,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? NOXFUNDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    investor: { value: input.investor ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    listing: { value: input.listing ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.investor),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.listing),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getPostInvestorListingInstructionDataEncoder().encode(
      args as PostInvestorListingInstructionDataArgs,
    ),
    programAddress,
  } as PostInvestorListingInstruction<
    TProgramAddress,
    TAccountInvestor,
    TAccountConfig,
    TAccountListing,
    TAccountSystemProgram
  >);
}

export type ParsedPostInvestorListingInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    investor: TAccountMetas[0];
    config: TAccountMetas[1];
    listing: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: PostInvestorListingInstructionData;
};

export function parsePostInvestorListingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPostInvestorListingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      investor: getNextAccount(),
      config: getNextAccount(),
      listing: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getPostInvestorListingInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
