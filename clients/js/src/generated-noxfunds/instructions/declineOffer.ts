/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { NOXFUNDS_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const DECLINE_OFFER_DISCRIMINATOR = new Uint8Array([
  204, 98, 121, 232, 122, 145, 35, 96,
]);

export function getDeclineOfferDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DECLINE_OFFER_DISCRIMINATOR,
  );
}

export type DeclineOfferInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountTrader extends string | AccountMeta<string> = string,
  TAccountOffer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTrader extends string
        ? ReadonlySignerAccount<TAccountTrader> &
            AccountSignerMeta<TAccountTrader>
        : TAccountTrader,
      TAccountOffer extends string
        ? WritableAccount<TAccountOffer>
        : TAccountOffer,
      ...TRemainingAccounts,
    ]
  >;

export type DeclineOfferInstructionData = {
  discriminator: ReadonlyUint8Array;
  reason: string;
};

export type DeclineOfferInstructionDataArgs = { reason: string };

export function getDeclineOfferInstructionDataEncoder(): Encoder<DeclineOfferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["reason", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: DECLINE_OFFER_DISCRIMINATOR }),
  );
}

export function getDeclineOfferInstructionDataDecoder(): Decoder<DeclineOfferInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["reason", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getDeclineOfferInstructionDataCodec(): Codec<
  DeclineOfferInstructionDataArgs,
  DeclineOfferInstructionData
> {
  return combineCodec(
    getDeclineOfferInstructionDataEncoder(),
    getDeclineOfferInstructionDataDecoder(),
  );
}

export type DeclineOfferInput<
  TAccountTrader extends string = string,
  TAccountOffer extends string = string,
> = {
  trader: TransactionSigner<TAccountTrader>;
  offer: Address<TAccountOffer>;
  reason: DeclineOfferInstructionDataArgs["reason"];
};

export function getDeclineOfferInstruction<
  TAccountTrader extends string,
  TAccountOffer extends string,
  TProgramAddress extends Address = typeof NOXFUNDS_PROGRAM_ADDRESS,
>(
  input: DeclineOfferInput<TAccountTrader, TAccountOffer>,
  config?: { programAddress?: TProgramAddress },
): DeclineOfferInstruction<TProgramAddress, TAccountTrader, TAccountOffer> {
  // Program address.
  const programAddress = config?.programAddress ?? NOXFUNDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trader: { value: input.trader ?? null, isWritable: false },
    offer: { value: input.offer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.trader), getAccountMeta(accounts.offer)],
    data: getDeclineOfferInstructionDataEncoder().encode(
      args as DeclineOfferInstructionDataArgs,
    ),
    programAddress,
  } as DeclineOfferInstruction<TProgramAddress, TAccountTrader, TAccountOffer>);
}

export type ParsedDeclineOfferInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    trader: TAccountMetas[0];
    offer: TAccountMetas[1];
  };
  data: DeclineOfferInstructionData;
};

export function parseDeclineOfferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDeclineOfferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { trader: getNextAccount(), offer: getNextAccount() },
    data: getDeclineOfferInstructionDataDecoder().decode(instruction.data),
  };
}
