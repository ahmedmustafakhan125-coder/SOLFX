/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { NOXFUNDS_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const ABANDON_EVALUATION_DISCRIMINATOR = new Uint8Array([
  115, 34, 15, 195, 27, 14, 4, 206,
]);

export function getAbandonEvaluationDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ABANDON_EVALUATION_DISCRIMINATOR,
  );
}

export type AbandonEvaluationInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountTrader extends string | AccountMeta<string> = string,
  TAccountEvaluation extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTrader extends string
        ? ReadonlySignerAccount<TAccountTrader> &
            AccountSignerMeta<TAccountTrader>
        : TAccountTrader,
      TAccountEvaluation extends string
        ? WritableAccount<TAccountEvaluation>
        : TAccountEvaluation,
      ...TRemainingAccounts,
    ]
  >;

export type AbandonEvaluationInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AbandonEvaluationInstructionDataArgs = {};

export function getAbandonEvaluationInstructionDataEncoder(): FixedSizeEncoder<AbandonEvaluationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ABANDON_EVALUATION_DISCRIMINATOR }),
  );
}

export function getAbandonEvaluationInstructionDataDecoder(): FixedSizeDecoder<AbandonEvaluationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAbandonEvaluationInstructionDataCodec(): FixedSizeCodec<
  AbandonEvaluationInstructionDataArgs,
  AbandonEvaluationInstructionData
> {
  return combineCodec(
    getAbandonEvaluationInstructionDataEncoder(),
    getAbandonEvaluationInstructionDataDecoder(),
  );
}

export type AbandonEvaluationInput<
  TAccountTrader extends string = string,
  TAccountEvaluation extends string = string,
> = {
  trader: TransactionSigner<TAccountTrader>;
  evaluation: Address<TAccountEvaluation>;
};

export function getAbandonEvaluationInstruction<
  TAccountTrader extends string,
  TAccountEvaluation extends string,
  TProgramAddress extends Address = typeof NOXFUNDS_PROGRAM_ADDRESS,
>(
  input: AbandonEvaluationInput<TAccountTrader, TAccountEvaluation>,
  config?: { programAddress?: TProgramAddress },
): AbandonEvaluationInstruction<
  TProgramAddress,
  TAccountTrader,
  TAccountEvaluation
> {
  // Program address.
  const programAddress = config?.programAddress ?? NOXFUNDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trader: { value: input.trader ?? null, isWritable: false },
    evaluation: { value: input.evaluation ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.trader),
      getAccountMeta(accounts.evaluation),
    ],
    data: getAbandonEvaluationInstructionDataEncoder().encode({}),
    programAddress,
  } as AbandonEvaluationInstruction<
    TProgramAddress,
    TAccountTrader,
    TAccountEvaluation
  >);
}

export type ParsedAbandonEvaluationInstruction<
  TProgram extends string = typeof NOXFUNDS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    trader: TAccountMetas[0];
    evaluation: TAccountMetas[1];
  };
  data: AbandonEvaluationInstructionData;
};

export function parseAbandonEvaluationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAbandonEvaluationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { trader: getNextAccount(), evaluation: getNextAccount() },
    data: getAbandonEvaluationInstructionDataDecoder().decode(instruction.data),
  };
}
