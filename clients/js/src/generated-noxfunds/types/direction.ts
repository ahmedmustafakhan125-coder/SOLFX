/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getEnumDecoder,
  getEnumEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Which way the trader is betting.
 *
 * The on-chain mirror of `solfx_math::Direction`. The maths crate deliberately carries no
 * Anchor derives — that is what keeps it testable without the BPF toolchain — so the two
 * types meet here and nowhere else.
 */
export enum Direction {
  Long,
  Short,
}

export type DirectionArgs = Direction;

export function getDirectionEncoder(): FixedSizeEncoder<DirectionArgs> {
  return getEnumEncoder(Direction);
}

export function getDirectionDecoder(): FixedSizeDecoder<Direction> {
  return getEnumDecoder(Direction);
}

export function getDirectionCodec(): FixedSizeCodec<DirectionArgs, Direction> {
  return combineCodec(getDirectionEncoder(), getDirectionDecoder());
}
