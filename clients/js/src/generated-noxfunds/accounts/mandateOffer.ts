/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getOfferStateDecoder,
  getOfferStateEncoder,
  type OfferState,
  type OfferStateArgs,
} from "../types";

export const MANDATE_OFFER_DISCRIMINATOR = new Uint8Array([
  20, 239, 145, 190, 228, 246, 19, 141,
]);

export function getMandateOfferDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MANDATE_OFFER_DISCRIMINATOR,
  );
}

export type MandateOffer = {
  discriminator: ReadonlyUint8Array;
  investor: Address;
  /**
   * The one trader who may accept. Offers are addressed, not open to the highest bidder —
   * an open offer would let any trader with a profile take capital an investor had picked
   * someone specific for.
   */
  trader: Address;
  seq: number;
  /** Escrowed at `["offer_vault", offer]` from the moment the offer is posted. */
  principal: bigint;
  maxTradeNotional: bigint;
  maxTotalNotional: bigint;
  maxDrawdownBps: number;
  maxDailyLossBps: number;
  maxRiskPerTradeBps: number;
  maxStopDistanceBps: number;
  maxConcurrentPositions: number;
  allowedMarkets: bigint;
  minHoldSlots: bigint;
  traderSplitBps: number;
  note: ReadonlyUint8Array;
  noteLen: number;
  /**
   * After this, the trader can no longer accept and the investor can always revoke.
   *
   * An offer with no expiry is capital an investor can lose track of; an expiry the trader
   * can ignore is not an expiry. Acceptance checks it against the cluster clock.
   */
  expiresAt: bigint;
  state: OfferState;
  createdAt: bigint;
  bump: number;
  vaultBump: number;
  /**
   * The trader's answer when they decline. Empty otherwise.
   *
   * On the offer rather than in an account of its own, so the reason is read in the same
   * place as the terms it refers to, and so declining costs the trader no rent.
   */
  reply: ReadonlyUint8Array;
  replyLen: number;
  reserved: ReadonlyUint8Array;
};

export type MandateOfferArgs = {
  investor: Address;
  /**
   * The one trader who may accept. Offers are addressed, not open to the highest bidder —
   * an open offer would let any trader with a profile take capital an investor had picked
   * someone specific for.
   */
  trader: Address;
  seq: number;
  /** Escrowed at `["offer_vault", offer]` from the moment the offer is posted. */
  principal: number | bigint;
  maxTradeNotional: number | bigint;
  maxTotalNotional: number | bigint;
  maxDrawdownBps: number;
  maxDailyLossBps: number;
  maxRiskPerTradeBps: number;
  maxStopDistanceBps: number;
  maxConcurrentPositions: number;
  allowedMarkets: number | bigint;
  minHoldSlots: number | bigint;
  traderSplitBps: number;
  note: ReadonlyUint8Array;
  noteLen: number;
  /**
   * After this, the trader can no longer accept and the investor can always revoke.
   *
   * An offer with no expiry is capital an investor can lose track of; an expiry the trader
   * can ignore is not an expiry. Acceptance checks it against the cluster clock.
   */
  expiresAt: number | bigint;
  state: OfferStateArgs;
  createdAt: number | bigint;
  bump: number;
  vaultBump: number;
  /**
   * The trader's answer when they decline. Empty otherwise.
   *
   * On the offer rather than in an account of its own, so the reason is read in the same
   * place as the terms it refers to, and so declining costs the trader no rent.
   */
  reply: ReadonlyUint8Array;
  replyLen: number;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link MandateOfferArgs} account data. */
export function getMandateOfferEncoder(): FixedSizeEncoder<MandateOfferArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["investor", getAddressEncoder()],
      ["trader", getAddressEncoder()],
      ["seq", getU8Encoder()],
      ["principal", getU64Encoder()],
      ["maxTradeNotional", getU64Encoder()],
      ["maxTotalNotional", getU64Encoder()],
      ["maxDrawdownBps", getU16Encoder()],
      ["maxDailyLossBps", getU16Encoder()],
      ["maxRiskPerTradeBps", getU16Encoder()],
      ["maxStopDistanceBps", getU16Encoder()],
      ["maxConcurrentPositions", getU8Encoder()],
      ["allowedMarkets", getU128Encoder()],
      ["minHoldSlots", getU64Encoder()],
      ["traderSplitBps", getU16Encoder()],
      ["note", fixEncoderSize(getBytesEncoder(), 180)],
      ["noteLen", getU8Encoder()],
      ["expiresAt", getI64Encoder()],
      ["state", getOfferStateEncoder()],
      ["createdAt", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["vaultBump", getU8Encoder()],
      ["reply", fixEncoderSize(getBytesEncoder(), 180)],
      ["replyLen", getU8Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: MANDATE_OFFER_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link MandateOffer} account data. */
export function getMandateOfferDecoder(): FixedSizeDecoder<MandateOffer> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["investor", getAddressDecoder()],
    ["trader", getAddressDecoder()],
    ["seq", getU8Decoder()],
    ["principal", getU64Decoder()],
    ["maxTradeNotional", getU64Decoder()],
    ["maxTotalNotional", getU64Decoder()],
    ["maxDrawdownBps", getU16Decoder()],
    ["maxDailyLossBps", getU16Decoder()],
    ["maxRiskPerTradeBps", getU16Decoder()],
    ["maxStopDistanceBps", getU16Decoder()],
    ["maxConcurrentPositions", getU8Decoder()],
    ["allowedMarkets", getU128Decoder()],
    ["minHoldSlots", getU64Decoder()],
    ["traderSplitBps", getU16Decoder()],
    ["note", fixDecoderSize(getBytesDecoder(), 180)],
    ["noteLen", getU8Decoder()],
    ["expiresAt", getI64Decoder()],
    ["state", getOfferStateDecoder()],
    ["createdAt", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["vaultBump", getU8Decoder()],
    ["reply", fixDecoderSize(getBytesDecoder(), 180)],
    ["replyLen", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

/** Gets the codec for {@link MandateOffer} account data. */
export function getMandateOfferCodec(): FixedSizeCodec<
  MandateOfferArgs,
  MandateOffer
> {
  return combineCodec(getMandateOfferEncoder(), getMandateOfferDecoder());
}

export function decodeMandateOffer<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<MandateOffer, TAddress>;
export function decodeMandateOffer<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<MandateOffer, TAddress>;
export function decodeMandateOffer<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<MandateOffer, TAddress> | MaybeAccount<MandateOffer, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getMandateOfferDecoder(),
  );
}

export async function fetchMandateOffer<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<MandateOffer, TAddress>> {
  const maybeAccount = await fetchMaybeMandateOffer(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeMandateOffer<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<MandateOffer, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeMandateOffer(maybeAccount);
}

export async function fetchAllMandateOffer(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<MandateOffer>[]> {
  const maybeAccounts = await fetchAllMaybeMandateOffer(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeMandateOffer(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<MandateOffer>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeMandateOffer(maybeAccount));
}

export function getMandateOfferSize(): number {
  return 545;
}
