/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const NOX_CONFIG_DISCRIMINATOR = new Uint8Array([
  115, 160, 106, 28, 4, 223, 249, 200,
]);

export function getNoxConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(NOX_CONFIG_DISCRIMINATOR);
}

export type NoxConfig = {
  discriminator: ReadonlyUint8Array;
  admin: Address;
  /** May pause, may not move funds. Same split as SolFX's guardian. */
  guardian: Address;
  /** Where the 5% performance fee and forfeited stakes accumulate. */
  treasury: Address;
  /**
   * The venue this program trades on. Checked on every CPI so a mandate cannot be pointed
   * at an impostor program that happens to share an instruction layout.
   */
  solfxProgram: Address;
  usdcMint: Address;
  /** Protocol share of **gross** profit. 500 = 5%. */
  protocolFeeBps: number;
  paused: boolean;
  bump: number;
  reserved: ReadonlyUint8Array;
};

export type NoxConfigArgs = {
  admin: Address;
  /** May pause, may not move funds. Same split as SolFX's guardian. */
  guardian: Address;
  /** Where the 5% performance fee and forfeited stakes accumulate. */
  treasury: Address;
  /**
   * The venue this program trades on. Checked on every CPI so a mandate cannot be pointed
   * at an impostor program that happens to share an instruction layout.
   */
  solfxProgram: Address;
  usdcMint: Address;
  /** Protocol share of **gross** profit. 500 = 5%. */
  protocolFeeBps: number;
  paused: boolean;
  bump: number;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link NoxConfigArgs} account data. */
export function getNoxConfigEncoder(): FixedSizeEncoder<NoxConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
      ["guardian", getAddressEncoder()],
      ["treasury", getAddressEncoder()],
      ["solfxProgram", getAddressEncoder()],
      ["usdcMint", getAddressEncoder()],
      ["protocolFeeBps", getU16Encoder()],
      ["paused", getBooleanEncoder()],
      ["bump", getU8Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 64)],
    ]),
    (value) => ({ ...value, discriminator: NOX_CONFIG_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link NoxConfig} account data. */
export function getNoxConfigDecoder(): FixedSizeDecoder<NoxConfig> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
    ["guardian", getAddressDecoder()],
    ["treasury", getAddressDecoder()],
    ["solfxProgram", getAddressDecoder()],
    ["usdcMint", getAddressDecoder()],
    ["protocolFeeBps", getU16Decoder()],
    ["paused", getBooleanDecoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 64)],
  ]);
}

/** Gets the codec for {@link NoxConfig} account data. */
export function getNoxConfigCodec(): FixedSizeCodec<NoxConfigArgs, NoxConfig> {
  return combineCodec(getNoxConfigEncoder(), getNoxConfigDecoder());
}

export function decodeNoxConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<NoxConfig, TAddress>;
export function decodeNoxConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<NoxConfig, TAddress>;
export function decodeNoxConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<NoxConfig, TAddress> | MaybeAccount<NoxConfig, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getNoxConfigDecoder(),
  );
}

export async function fetchNoxConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<NoxConfig, TAddress>> {
  const maybeAccount = await fetchMaybeNoxConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeNoxConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<NoxConfig, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeNoxConfig(maybeAccount);
}

export async function fetchAllNoxConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<NoxConfig>[]> {
  const maybeAccounts = await fetchAllMaybeNoxConfig(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeNoxConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<NoxConfig>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeNoxConfig(maybeAccount));
}

export function getNoxConfigSize(): number {
  return 236;
}
